# N36 — SPEC 재확정 변경 요약과 질문

## 바꾼 절

- `SPEC.md:18-22` R1-2 — description의 트리 문구·번호·네 줄 문장 복제 요구를 없애고 우선순위·경계(5개 초과 수치, 세 예외 문장)·전환의 의미를 짧은 동등 표현으로 담게 함. 파일을 고치지 않는 직접 처리에 필요한 승인·검증·실패 보고와 수행 중 전환 조건(2·3·5 전환, Step 0 발동, R1-7 발동)은 description에 두도록 명시. 본문 코드블록 원문 유지. R1-10은 그대로.
- `SPEC.md:237-242` A6 「담는다」 — 원문 스크립트 대조를 조건별 포함 확인(조건↔인용 대응을 결과 보고에 남기고, 인용의 description 내 존재만 스크립트 확인)으로 바꿈. 나열된 의무는 동등한 짧은 표현 허용. 브레인스토밍 4표현은 트리거 문구라 "의무"로 보지 않고 문구 그대로 유지.
- `SPEC.md:243` A6 1,024자·`<`/`>`·YAML 검사 — 변경 없음.
- `SPEC.md:245-252` A6 트리거 평가 — `run_eval.py` PowerShell 명령을 격리 임시 폴더 실제 스킬 + `claude -p … stream-json` 첫 도구 호출 판정 방식으로 교체(아래 항목 1).
- `SPEC.md:253-255` A6 평가 세트·재실행·통과 — `run_eval.py`·`--runs-per-query`·`--trigger-threshold` 표현만 새 방법 기준으로 바꿈. 세트 조건, 재실행 대상, 3회 중 2회, `summary.failed` = 0은 유지.
- `SPEC.md:162` C3 — "claude `skill-creator` 트리거 평가"를 "A6의 트리거 평가"로 바꿈(항목 1과 묶임).

## 1. 측정 명령 변경 — 수락·거부

A6의 트리거 평가를 사용자가 지시한 "실제 환경과 같은 방식"으로 바꿔 반영했다(`SPEC.md:245-252`, `SPEC.md:162`).

- 함께 바뀐 점: 홈의 `skill-trigger-eval` 사본 전제와 "사본이 없으면 미검증 실패" 조건 삭제, codex 세션의 `--model` 생략 문구 삭제(`<모델>`은 유효한 Claude 모델 ID로 지정하고 기록), 출력·실행 제어 옵션(`--verbose` 등) 추가 허용, 초기 정보에 대상 스킬 없음·설치본 paseo-toolkit 노출·오류 종료를 질의 실패로 정의.
- 수락하면 AGENTS.md §6의 "claude `skill-creator` 트리거 평가(`~/.skywork/skill-trigger-eval/` 사본 실행)" 대신 이 방법을 이번 작업의 A6 판정 방법으로 쓰게 된다.
- 거부하면 `SPEC.md:245-252`와 `SPEC.md:162`를 이전 `run_eval.py` 명령과 C3 문구로 되돌린다. 「담는다」 변경(선택지 1)은 영향받지 않는다.

답: 수락 / 거부

## 2. 새 질문

1. 자문 N35 선택지 1에는 "두 호스트(claude·codex)의 실제 노출·평가 추가"가 있었으나 전달된 사용자 결정에는 없어 반영하지 않았다. A6에 codex 쪽 발동 평가를 추가할지 정해 달라. 추가하면 수용 판정 범위가 넓어진다. (추가 / 추가하지 않음)

## MILESTONE01-Tasks.md 충돌 줄 (고치지 않음)

- `MILESTONE01-Tasks.md:27` — T01 할 일: description에 트리를 "같은 문구·번호·순서로" 둔다(R1-2와 충돌).
- `MILESTONE01-Tasks.md:134` — T12 할 일: description에 트리 머리·여섯 항목·네 줄 문장을 "문구·순서 그대로" 담는다, "홈의 수정된 트리거 평가 사본으로" 평가(R1-2·A6과 충돌).
- `MILESTONE01-Tasks.md:136` — T12 완료 조건 끝의 "사본 또는 Claude CLI 인증이 없으면 미검증 실패"(항목 1 수락 시 충돌).
- `MILESTONE01-Tasks.md:138` — T12 검증 방법: "스펙 A6의 아래 PowerShell 명령을 그대로", codex 세션 `--model` 생략(항목 1 수락 시 충돌).
- `MILESTONE01-Tasks.md:140-158` — `run_eval` PowerShell 코드블록(항목 1 수락 시 충돌).
- `MILESTONE01-Tasks.md:160` — `--runs-per-query 3` 재실행 지시(항목 1 수락 시 충돌).
