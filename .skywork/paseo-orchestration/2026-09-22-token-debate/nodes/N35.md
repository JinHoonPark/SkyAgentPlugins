# N35 — description 길이 자문

## 결론

**줄이는 방향을 권고한다.** 987자는 공용 규격상 유효하지만 상한 1,024자의 96.4%이며, 권장하는 간결한 발견용 설명과는 거리가 있다. 그렇다고 길이만으로 토큰 효율화 실패나 발동 불량이라고 판정할 근거는 없다. 우선 선택지 1의 의미 보존 축약을 검토하고 두 호스트에서 검증하는 편이 합리적이다.
이는 스펙 변경 제안이며 확정·구현하지 않았다. 현재 길어진 구현은 판정 트리의 원문 복제를 요구한 스펙에 따른 결과다: `.skywork/paseo-orchestration/2026-09-22-token-debate/SPEC.md:18`, 같은 파일 `:46`, `:233`.

## 핵심 근거와 위치

### 1. 공식 한도와 권장 — 유효 범위와 권장 분량은 다르다

| 구분 | 확인된 기준 | 판단과 근거 |
| --- | --- | --- |
| Agent Skills 공용 규격 | description 1~1,024자. 기능·적용 시점·관련 키워드를 설명한다. 메타데이터는 약 100토큰을 예시로 든다. | 987자는 유효하나 약 100토큰은 상한이나 한국어 문자 수 권장이 아니다. [공식 규격](https://agentskills.io/specification#description-field), [점진적 로드](https://agentskills.io/specification#progressive-disclosure) |
| Claude 공통 작성 지침 | description 최대 1,024자. 간결하게 기능과 발동 맥락을 쓰고 구현 상세는 본문에 둔다. | 긴 판정 절차 전체를 반드시 description에 쓰라는 지침은 없다. [Anthropic 작성 지침](https://platform.claude.com/docs/en/agents-and-tools/agent-skills/best-practices#skill-structure) |
| Claude Code 호스트 | 현재 공식 문서는 목록에서 description과 when_to_use를 합쳐 기본 1,536자에 잘라 싣는다고 명시한다. 전체 목록 예산은 컨텍스트의 1%이며 부족하면 description을 축약·제외한다. | 1,536자는 **목록 표시 한도**이며 공용 규격의 1,024자와 다른 개념이다. CLI 파서의 거부 한도가 1,024자라고 이 문서만으로 단정할 수 없다. [Claude Code frontmatter](https://code.claude.com/docs/en/skills#frontmatter-reference), [목록 예산](https://code.claude.com/docs/en/skills#skill-descriptions-are-cut-short) |
| Codex 계열 | 공식 플러그인 검증 문서는 description 1,024자 이하를 요구한다. 설치된 Codex 작성 도구 검증기도 같은 한도다. | 이 공용 플러그인의 보수적 호환 기준은 1,024자다. 로컬 CLI 파서의 경계값 거부 실험은 하지 않았다. [OpenAI 검증 오류](https://developers.openai.com/plugins/deploy/submission-errors), `C:/Users/magedoga/.codex/skills/.system/skill-creator/scripts/quick_validate.py:87` |

- Codex 설치 지침은 발견 비용을 낮추고 구별되는 조건을 간결하게 쓰며, 상세 절차·예시·운영 모드를 description에 열거하지 말라고 한다: `C:/Users/magedoga/.codex/skills/.system/skill-creator/SKILL.md:24`, `:56`, `:191`, `:199`.
- Claude 공식 skill-creator의 로컬 마켓플레이스 사본은 모든 적용 조건을 description에 두고 미발동 방지를 위해 다소 강하게 쓰라고 한다. 메타데이터 약 100단어를 제시하되 필요하면 길어져도 된다고 명시한다. **100단어와 Agent Skills의 100토큰은 다르며, 어느 쪽도 1,000자 권장이 아니다.** `C:/Users/magedoga/.claude/plugins/marketplaces/claude-plugins-official/plugins/skill-creator/skills/skill-creator/SKILL.md:67`, `:89`, `:93`; 같은 디렉터리 `scripts/quick_validate.py:83`은 1,024자 초과를 거부한다.
- 설치 상태 한계: 이 PC의 `C:/Users/magedoga/.claude/plugins/installed_plugins.json:3`에는 skill-creator 등록이 없고 cache에도 해당 설치본을 찾지 못했다. 따라서 위 Claude 자료는 **설치·활성화된 사본으로 확인된 것이 아니라 공식 마켓플레이스에 내려받은 사본**이다. 이를 설치본이라고 표현하지 않는다.
- 판단: 공식 평균·분포 자료는 확인하지 못했으므로 “흔한 길이”인지는 미확인이다. 정확한 표현은 **허용되지만 상한에 가까운 긴 설명**이다. 간결함을 이유로 필요한 발동 경계까지 지우라는 뜻은 아니다.

### 2. 토큰 효율 — 상시 비용과 조건부 절감을 함께 계산해야 한다

- 현행 파일을 UTF-8로 읽어 YAML 파싱한 결과 description은 987자·2,320바이트다. LF 기준 파일 전체는 18,576자이고 frontmatter 뒤 부분은 17,540자다. 따라서 요청의 “본문 약 18,600자”는 엄밀히는 **로드하는 SKILL.md 전체**에 해당한다: `plugins/paseo-toolkit/skills/agent-orchestration/SKILL.md:3` 및 파일 전체 직접 측정.
- 기준값에 작은 불일치가 있다. 사용자가 준 325자를 쓰면 증가량은 662자다. 다만 `5e833be…` 설치 기록이 있는 캐시 사본은 따옴표 포함 원시 값 325자, YAML 파싱 값 323자였다. 현재도 같은 방식으로 재면 989자/987자이므로 동일 기준 증가량은 **664자**다. Git 객체와 캐시의 동일성은 직접 검증하지 않았다. 근거: `C:/Users/magedoga/.claude/plugins/installed_plugins.json:21`, `C:/Users/magedoga/.claude/plugins/cache/sky-agent-plugins/paseo-toolkit/0.11.0/skills/agent-orchestration/SKILL.md:3`.
- 상시 비용이라는 방향은 맞다. 메타데이터는 발동 전에 제공되고 본문은 선택 후 로드된다. 다만 **모든 세션에서 모든 description 전문이 보장되는 것은 아니다.** 최신 Codex 공식 문서도 초기 목록 예산을 컨텍스트의 2%, 크기 미상 시 8,000자로 설명하며 축약·스킬 누락 가능성을 명시한다. [OpenAI 공식 스킬 문서](https://learn.chatgpt.com/docs/build-skills)
- **가정 기반 추정, 실제 토큰 측정 아님:** description 증가분과 생략할 파일의 토큰/문자 비율을 같은 `r`로 두고, 독립 초기 컨텍스트 `N`개 중 `K`개에서 파일 로드 1회를 피한다고 하자. 반복 입력·캐시·워커·다른 변경을 제외하면 순증가량은 `r × (664N − 18,576K)`다. 손익분기는 `K/N > 3.57%`다. 사용자 기준 증가량 662자를 쓰면 3.56%로 결론은 같다.
- 예시로 `r=1`토큰/문자를 **계산 편의상 가정**하면 상시 증가 664토큰, 로드 1회 생략 18,576토큰이다. 100개 컨텍스트 중 4개에서 로드를 피하면 7,904토큰 상당 순감소다. 이 `r`은 한국어 토크나이저의 실측값이나 공식 환산율이 아니다.
- 위 계산은 비용 규모를 비교한 모형이다. 이전 캐시 파일 전체는 18,744자로 현재와 다르고, 실제 이전 실행이 무엇을 얼마나 읽었는지도 측정하지 않았다. 3.57%를 운영 환경의 검증된 손익분기로 사용하면 안 된다.
- 실제 합산 입력 토큰에서는 각 호출에 남아 있는 메타데이터와 본문의 반복 노출을 각각 세어야 한다. 워커마다 추가되는 컨텍스트도 합산한다. 생략 워커의 입력·출력·도구 왕복 절감은 별도 이득이고, 직접 처리에 추가된 리더 작업과 직접 수정 뒤 검토 워커 비용도 반영해야 한다. 파일 미수정 직접 처리는 로드 0회지만 직접 수정은 검토 워커 1개가 남는다: `.skywork/paseo-orchestration/2026-09-22-token-debate/SPEC.md:186`, `:187`.
- 판단: **조건부 로드·위임 생략은 목적에 부합할 수 있다. 그러나 동일한 발동 품질을 더 짧은 설명으로 얻을 수 있다면 987자를 유지할 경제적 이유는 약하다.** 캐시 할인·모델별 가격·직접 처리 빈도가 미측정이므로 전체 비용 절감률은 미입증이다. 세션·워커 합산 실측을 요구하는 기존 수용 조건도 이 구분을 이미 한다: `.skywork/paseo-orchestration/2026-09-22-token-debate/SPEC.md:274`, `:275`.

### 3. 길이·구조와 발동 신뢰도

- **장점에 대한 추론:** 트리 전문과 예외가 선택 단계에 있으면 이름 없는 파일 조사, 단순 위치 검색, 직접 지시 우선순위를 본문 없이 구분할 정보가 생긴다. R1-10을 지키면서 본문 읽기 0회를 만들려면 필요한 선택 정보는 실제로 보이는 메타데이터에 있어야 한다. 근거는 현재 트리와 조건부 로드 구조다: `plugins/paseo-toolkit/skills/agent-orchestration/SKILL.md:3`, `.skywork/paseo-orchestration/2026-09-22-token-debate/SPEC.md:46`.
- **확인된 위험:** 두 호스트 모두 목록 예산에 따른 축약 가능성을 문서화했다. 따라서 파일상 1,024자 통과는 판정 규칙의 전문 전달 보장이 아니다. 앞의 공식 Claude Code·OpenAI 문서는 핵심 용도·키워드를 앞에 두도록 권한다.
- **현재 구조에 대한 추론:** 우선순위 예외, 순서 있는 트리, 수행 중 전환, 행동 전 발동, 작업 뒤 발동, 실패 보고가 한 문단에 섞여 있다. 뒤쪽이 생략되면 독립 검토·재위임 금지·직접 처리 의무를 전달하지 못할 수 있다. 실제로 잘렸거나 위반됐다는 관측은 없으며, 장문 자체 때문에 정확도가 떨어진다는 실험 결과도 없다.
- **추측:** 전문 복제는 누락을 줄이는 대신 조건 간 우선순위를 혼동할 가능성을 늘릴 수 있다. 반대로 축약이 너무 강하면 경계가 흐려져 오발동·미발동이 늘 수 있다. 어느 쪽 효과가 우세한지는 길이만으로 결정할 수 없다.
- 검증은 긍정·근접 부정 사례, 직접 지시와 위임 충돌, 수행 중 전환, 작업 뒤 검토를 포함해야 한다. Claude 지침도 근접 부정 사례·반복 평가를 강조한다: `C:/Users/magedoga/.claude/plugins/marketplaces/claude-plugins-official/plugins/skill-creator/skills/skill-creator/SKILL.md:356`, `:394`.
- 기존 A6은 Claude 평가 스크립트를 쓰므로 통과해도 Codex의 선택 품질까지 입증하지 않는다. 두 호스트의 실제 노출 description과 대상 모델별 결과를 따로 확인해야 한다. 이는 추가 검증 권고이며 이번 자문에서 실행하지 않았다: `.skywork/paseo-orchestration/2026-09-22-token-debate/SPEC.md:236`, `:257`; [모델별 테스트 지침](https://platform.claude.com/docs/en/agents-and-tools/agent-skills/best-practices#test-with-all-models-you-plan-to-use).

### 4. 줄이는 선택지 — 최대 2개, 선택지 1 우선 권고

| 선택지 | R1-2·R1-10·A6 변경 제안 | 장점 | 단점 |
| --- | --- | --- | --- |
| 1. 선택 의미를 보존해 축약 | **R1-2:** 본문 트리는 원문을 유지하되 description은 문구·번호의 복제 대신 우선순위·경계·전환의 의미 보존을 허용한다. **R1-10:** description만으로 판정하고 미수정 직접 처리에서는 읽지 않는 원칙을 유지한다. **A6:** 원문 문자열 대조를 조건별 포함 확인과 행동 평가로 바꾼다. 나열된 의무도 동등한 짧은 표현을 허용하고, 순수한 발동 후 절차만 본문으로 옮긴다. 1,024자·YAML 검사는 유지하고 두 호스트의 실제 노출·평가를 추가한다. | 변경 범위가 작고 직접 처리 로드 0회를 보존한다. | 의미 동등성을 문자열 검사만으로 입증할 수 없다. 전문 전달이 보장되지 않는 문제는 별도 검증이 필요하다. |
| 2. 짧은 진입 본문에서 최종 판정 | **R1-2:** 트리 원문은 짧아진 SKILL.md 본문에만 둔다. **R1-10:** description은 후보 선택, 짧은 본문이 최종 판정을 담당하도록 바꾼다. **A6:** description 필수 목록을 후보 트리거·주요 제외 조건으로 좁히고, 상세 우선순위·전환·의무는 진입 본문에서 검증한다. 기존 상세 실행 절차는 참조로 이동한다. | 메타데이터를 크게 줄이면서 판정 트리를 읽기 쉬운 형태로 유지한다. | 직접 처리에도 작은 본문 로드가 생겨 A1의 로드 0자 조건도 바꿔야 한다. R3의 본문 유지 항목·참조 구조까지 영향이 커서 차선책이다. |

선택지 1에서도 **미발동 직접 처리에 필요한 승인·검증·실패 보고와 수행 중 전환 조건을 읽히지 않는 본문으로만 옮기면 안 된다.** 위임 여부 재질문 금지 같은 발동 후 실행 규칙과 이를 구분해야 한다. 외부 전역 지침으로 이동하려면 현재 비범위인 전역 설정 변경까지 별도 승인해야 하므로 여기서는 권하지 않는다: `.skywork/paseo-orchestration/2026-09-22-token-debate/SPEC.md:36`, `:46`, `:170`.

## 검증 기록

- 방법을 먼저 정한 뒤 파일 길이·YAML·두 작성 도구 검증기를 실행했다. 명령 `python -X utf8 -B C:/Users/magedoga/.codex/skills/.system/skill-creator/scripts/quick_validate.py plugins/paseo-toolkit/skills/agent-orchestration` → 종료 0, `Skill is valid!`.
- 명령 `python -X utf8 -B C:/Users/magedoga/.claude/plugins/marketplaces/claude-plugins-official/plugins/skill-creator/skills/skill-creator/scripts/quick_validate.py plugins/paseo-toolkit/skills/agent-orchestration` → 종료 0, `Skill is valid!`.
- 읽기 전용 인라인 Python(`python -X utf8 -`)의 `Path.read_text(encoding='utf-8')`, `yaml.safe_load`, `len`으로 위 문자·바이트 수를 측정했다. 이는 형식 검증이며 실제 발동 품질 검증은 아니다.

## 미확인 항목 및 이유

- 실제 Claude skill-creator 설치본: 등록·cache에서 찾지 못해 공식 마켓플레이스 사본으로 확인 범위를 한정했다.
- Git 커밋 `5e833be` 원문: Git 작업 금지로 직접 조회하지 않았다. 325자는 사용자 제공값이며 캐시에서는 따옴표 포함/제외 차이를 발견했다.
- 실제 모델 토큰 수·청구 비용·발동률·호스트별 전문 전달 여부: 이번 작업은 읽기 전용 자문이고 실행 비교 평가를 하지 않았다. 로컬 `tiktoken`도 없어 환산은 명시적 가정만 사용했다.
- 공식 문서는 2026-09-24 조회 기준이다. 문서의 최신 호스트 동작이 설치된 CLI 버전과 동일한지는 별도 확인하지 않았다. 규격 한도, 목록 표시 한도, 추천 분량을 구별해서 적용해야 한다.
