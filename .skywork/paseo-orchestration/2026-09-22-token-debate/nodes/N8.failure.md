# N8 실패 보고

고정 모델 `claude-opus-5`로 시작한 U1의 첫 `claude -p` 호출이 초기화 후 `401 authentication_failed` 재시도를 기록했다 (`spike-logs/u1-init.jsonl:1`, `spike-logs/u1-init.jsonl:2`). 사용자 지시에 따라 대체 모델을 사용하지 않고 해당 호출과 상위 실행 스크립트를 중단했다.

사후 스냅숏에는 다음 불변 조건 위반이 확인됐다.

- 네이티브 Git diff 해시가 실행 전 `e6c635db32e1730efef1624e7522352c87caf4ef`에서 실행 후 `ac4641bfa1d6cf438f0f0d71eb4272f28dcad2e0`로 변경됐다 (`spike-logs/snapshot-comparison.txt:3`, `spike-logs/snapshot-comparison.txt:7`).
- WSL `~/.claude/plugins/` 및 marketplace 아래 새 파일 215개와 Windows `~/.claude/` 새 파일 2개가 감지됐다 (`spike-logs/snapshot-comparison.txt:16`, `spike-logs/snapshot-comparison.txt:17`). 이는 허용된 WSL `~/.claude/projects/` 세션 기록 예외에 해당하지 않는다.

U1은 미판정, U2·U3은 미실행으로 종료했다. 실제 `claude -p` 호출 수는 1회다. 격리 fixture는 삭제됐으며 전역 파일은 추가로 변경·정리하지 않았다.
