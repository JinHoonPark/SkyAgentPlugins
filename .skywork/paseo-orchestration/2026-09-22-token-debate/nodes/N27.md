# N27 — T11 규칙 단위 의미 대조

## 결론과 범위

**A4·R9-1~3 통과.** `5e833be`의 `SKILL.md` 전체와 변경된 이전 판본 여섯 파일(`assets/review-briefing.md`, `references/FORM.md`, `references/brainstorming.md`, `references/failure-handling.md`, `references/level1.md`, `references/level2.md`)을 대조했다. 새 `references/system-prompt.md`는 변경 전 판본이 없고 새 위치 후보로만 확인했다. `presets.md`는 T11 대조 대상이 아니다.

변경 전 규칙 단위 **876건**, 대조표 **876행**이다. 판정: 동일 589, 축약 21, 통합 233, 의도 변경 R1 8·R1-7 7·R2 3·R6 7·R7 5·R8 2, 제거 R1 1. 의도 변경·제거는 허용된 R1·R2·R6·R7·R8에만 있다. 축약·통합은 이전 조건·예외·주체가 새 절 또는 그 절이 지목한 정본에 남는지 확인했다. 승인·검토 독립성·결과/실패 파일·실패 우선·그래프 형식과 기록 시점·발화 규칙은 보존됐다(`SKILL.md:12-16,46-53,224-243,257-265,269-279`; `references/level1.md:7-31`; `references/FORM.md:5-36,218-268`). R3-4의 본문 유지 항목도 대조표 검사에서 확인했다.

## 발견한 불일치와 수정

| 변경 전 규칙 | 누락과 한정 수정 |
| --- | --- |
| `5e833be:references/level2.md:140` | 스펙 문답 중계에서 질문·답변 이외의 스펙 본문과 작성자 분석을 제외하는 조건이 약해졌다. `plugins/paseo-toolkit/skills/agent-orchestration/references/level2.md:160`에 해당 제외 조건을 복원했다. |
| `5e833be:SKILL.md:339` | 검토 라운드 상한 소진 시 남은 지적과 각 라운드 결과 파일 경로를 보고한 뒤 멈추는 조건이 빠졌다. `plugins/paseo-toolkit/skills/agent-orchestration/references/level2.md:306`에 복원했다. |

그 밖의 미해결 불일치·누락은 **0건**이다. 원문 고정 문구, 두 `plugin.json` 버전, 확정 `SPEC.md`·`MILESTONE01-Tasks.md`는 수정하지 않았다. 스킬 본문은 **18,631자·304줄**로 이전 18,744자·499줄 이내다. 수정한 `level2.md`는 기존 CRLF와 BOM 없는 UTF-8을 유지했다.

## 실행 증빙

| 명령 | 결과 |
| --- | --- |
| `python -X utf8 .skywork/paseo-orchestration/2026-09-22-token-debate/rule-audit.py` | 종료 0. 누락 0, 새 위치·인용·판정·R3 본문 배치 검사 오류 0. 이전 판본은 스크립트가 `git show 5e833be:<경로>`로 직접 읽는다. |
| `python -X utf8 .skywork/paseo-orchestration/2026-09-22-token-debate/link-audit.py` | 종료 0. `source errors: 0`. |
| 스킬 디렉터리에서 `python -X utf8 scripts/graph_update.py --self-test` | 종료 0. G1~G11 모두 통과. |
| `git diff --check -- plugins/paseo-toolkit/skills/agent-orchestration` | 종료 0, 출력 없음. |
| `git diff --no-ext-diff -- plugins/paseo-toolkit/skills/agent-orchestration plugins/paseo-toolkit/references/presets.md` | 종료 0. 추적 변경 8파일(기존 스킬 7파일과 이전 태스크의 `presets.md`) 범위 확인. 신규 참조·스크립트는 별도로 파일 목록을 확인했다. |

미검증 항목은 없다. 실제 에이전트 실행은 T11의 정적 의미 대조 범위 밖이다.
