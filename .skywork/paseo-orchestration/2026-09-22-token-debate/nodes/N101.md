# N101 — paseo-toolkit 0.12.0(621e94b) vs 5e833be: 개선·악화·우려 분석

표기: `SKILL.md`·`refs/`·`assets/`·`graph_update.py`는 `plugins/paseo-toolkit/skills/agent-orchestration/` 기준(621e94b), `presets.md`는 `plugins/paseo-toolkit/references/presets.md`, `5e833be:` 접두는 변경 전 판본, `RUN/`은 `.skywork/paseo-orchestration/2026-09-22-token-debate/`, `N99`는 `RUN/nodes/N99.md`다. **[사실]** = 파일·로그·명령으로 확인, **[추측]** = 근거에서 추론.

## 결론

- 의도(조건부 적용으로 불필요한 로드·워커 제거, 재독 감소, 안전 동작 유지)에 대체로 부합한다. 가장 큰 개선은 직접 처리 경로에서 스킬 로드·워커가 0이 된 것(정적 29,002→0자, 실측 3/3)과 레벨 1·2 재독 감소다.
- 대신 안전 동작 가운데 **경미한 수정 예외**와 **description만으로 하는 판정**에 기대는 부분이 실측에서 흔들렸다(M4 r1 검토 누락, M3 생략 보고 2/3 누락). 비경미 직접 수정 경로는 정적 로드가 오히려 늘었다.
- 전체 토큰/비용 절감률은 입증되지 않았다(N99:8).

## 1. 개선된 점

**I1. 조건부 적용 — 무조건 위임 제거 (높음)**
- [사실] "모든 작업 요청에 착수하기 전에 반드시 이 스킬을 읽고"·"할 일만 적힌 요청은 전부 위임 대상"·"작업 크기로 예외를 두지 않는다"·"파일과 설정은 … 워커에 맡긴다"(5e833be:SKILL.md:3, :8-9, :23-24)가 모두 사라지고(현재 스킬·presets 검색 0건) 트리 1~6 판정으로 바뀌었다(SKILL.md:3, :18-31). team-lead notes도 같은 취지로 맞췄다(presets.md:214, N99:37-38).
- [사실] D0(직접 처리) 정적 로드 29,002→0자, Read 7→0, 워커 1→0(N99:49-50). 실측 A8-1 3/3: Skill 0회·`create_agent` 0회·정답, 세션 입력 약 55k 토큰(RUN/nodes/N73.md:5-11, N99:84-86).

**I2. Step 0 경량 경로 (높음)**
- [사실] 되돌리기 어려운 행동만 걸리면 워커·`GRAPH.md`·`list_profiles`·`list_workspaces` 없이 승인만 받는다(SKILL.md:12-16, refs/FORM.md:20). S0 로드 20,507→18,175자, Read 1→0(N99:52).
- [사실] 실측 A8-3 3/3 승인 전 멈춤·삭제 대상 해시 유지·프로필 조회/워커/그래프 0(RUN/nodes/N87.md:9-13). 무응답≠승인, 팀장 추가 행동은 항상 승인 대상이라는 승인 강도도 유지(SKILL.md:16).

**I3. 재독 금지·필요한 절만 읽기 (중간)**
- [사실] 무조건 "직전에 읽는다"(5e833be:SKILL.md:198, :299, :343-344, :425)가 "현재 컨텍스트에 없으면 필요한 절만" 조건형으로 바뀌었다(SKILL.md:34, :161, :211, :231, :268, :272, :291-292).
- [사실] L1 로드(재독 포함) 32,732→27,715자(−15.3%)·Read 11→8, L2 58,054→50,955자(−12.2%)·Read 20→19(N99:53-54). N31 실측에서 같은 참조 절 재독 0(N99:102).

**I4. `GRAPH.md` 갱신 스크립트화 (중간)**
- [사실] 파일을 읽고 고치던 생성·행 추가·열 변경이 명령 1회가 됐고(SKILL.md:35, N99:30), 팀장이 plugin.json을 읽어 쓰던 버전 줄(5e833be:refs/level1.md:23-29)을 스크립트가 쓴다(graph_update.py:90-102).
- [사실] 상태 5종·열 이름·`|`·중복 ID를 검증하고 임시 파일 교체로 부분 기록을 막는다(graph_update.py:49-61, :105-129, :203-233). self-test G1~G11 통과(N99:33).

**I5. 판정 통합·순차 워커 재사용 (중간)**
- [사실] L2 기동 워커 6→4(N99:54). 재사용은 같은 역할·대상, 순차 엣지, 승인 범위, 수신 가능의 네 조건을 모두 요구하고 검토 노드에는 쓰지 않는다(refs/level2.md:344-359).
- [사실] 한 `agentId`가 여러 행에 있을 때의 재개 판정 규칙을 새로 뒀다(refs/failure-handling.md:146, :227-233).

**I6. 스펙 질문 묶기 (중간)**
- [사실] "한 번에 서로 독립된 결정 하나만"(5e833be:refs/level2.md:138)이 독립 질문 번호 묶음으로 바뀌었다(SKILL.md:112-114). L2 질문 중계 3→1회(N99:54). 필수 답 전 진행 금지·미응답≠승인은 유지.

**I7. 직접 수정 뒤 독립 검토 신설 (높음, 안전)**
- [사실] 검토 개설 조건 셋→넷(5e833be:SKILL.md:115 → SKILL.md:107). 검토 입력을 사용자 요청 원문·확정 제약·대상 경로로 제한하고 독립성 3조건을 그대로 적용한다(SKILL.md:221-227, assets/review-briefing.md:7). 실측 M5 3/3 통과(N99:104).

**I8. 사용자 명시 전환·지시 충돌 처리 (중간)**
- [사실] 전환 문구 두 개가 description에 있고(SKILL.md:3), M4 다섯 시도 모두 전환 지시 뒤 첫 도구가 Skill이었으며 전환 전 수정은 보존됐다(RUN/nodes/N88.md:27, :29). 직접·워커 지시 충돌 시 확인 질문 A8-4 3/3(N99:36).

**I9. 트리거 정확도 (중간)**
- [사실] description 1,024자 이하·YAML 통과, 정해진 28질의 1차 28/28·경계 16질의 재실행 16/16(N99:34, :73-76).

**I10. 구조 정리 (낮음)**
- [사실] SKILL.md 499→304줄(본문 18,371→17,115자, −6.8%, 직접 계산; 줄 감소는 대부분 줄바꿈 합치기). 8절 상세는 refs/system-prompt.md로 분리했고 레벨 1·2 공통 `GRAPH.md` 규칙을 refs/level1.md 한 곳에 모았다. 링크·절 참조 오류 0, 규칙 876단위 누락 0(N99:31-32).

## 2. 나빠진 점

**W1. 경미한 수정 경로의 작성자·확인자 분리 약화와 예외 오적용 (높음)**
- [사실] 이전에는 수정은 워커가 하고 결과 인수는 팀장이 했다(5e833be:SKILL.md:23-24, :445). 지금 경미한 수정은 팀장이 쓰고 팀장이 예외 여부를 판정·확인한다(SKILL.md:41).
- [사실] 실측: M4 r1은 두 줄 변경을 한 줄 예외로 처리해 검토 0(RUN/nodes/N88.md:5, :9). M3 r2·r3는 스킬을 한 번도 부르지 않았고 생략 사실 보고가 빠졌다(RUN/nodes/N87.md:10-11, :13).
- [사실] description에는 "경미한 수정 생략은 이 스킬 기준으로 판정"만 있고(SKILL.md:3) 생략 사실·증빙 보고 의무는 본문에만 있다(SKILL.md:41). [추측] 그래서 본문을 읽지 않고 예외를 판정하는 경로가 열려 있다.

**W2. 비경미 직접 수정 경로의 비용 증가 (중간, 의도된 trade-off)**
- [사실] D1 정적 로드 29,002→31,173자(+7.5%), Read 7→10(N99:51). SPEC이 D1에 비교 조건을 두지 않았다(RUN/SPEC.md:192).
- [사실] 한 함수 수정(M5)의 세션 합계 입력이 994,700~1,128,606토큰(캐시 포함)·호출 22~24회·워커 1개였다(N99:104). [추측] 이전 판본으로 같은 과제를 돌린 기준선이 없어 동적 증감은 모른다. 작업을 팀장이 직접 하므로 팀장 세션도 커진다.

**W3. `python` 명령 의존이라는 새 실패 경로 (낮음)**
- [사실] 문서는 `python scripts/graph_update.py`를 지시하는데(SKILL.md:35, refs/failure-handling.md:13) 이 Mac에는 `python`이 없고 `python3`만 있다(`command -v python` 실패). M4 로그 3건에서 `command not found: python`이 났다(M4 r3는 `create` exit 127; RUN/final-live2/M4/r3/leader-session.jsonl:77, r2/leader-session.jsonl:68, r2/retry1/leader-session.jsonl:62). 모두 `python3`로 자가 복구했다.
- [사실] 팀장 세션 9개 중 7개가 `--help`를 따로 호출했다. `create`는 부모 디렉터리가 없으면 실패하므로(graph_update.py:110) 팀장이 `mkdir -p`를 먼저 불렀다(M5 로그). 도구 호출이 그만큼 늘어난다.

**W4. 상주 비용과 description 수정 여유 (낮음)**
- [사실] description이 323→1,010자(3.1배)로 늘어 워커를 포함한 모든 세션의 스킬 목록에 상주하고, 상한 1,024자까지 14자 남았다(N99:45, RUN/SPEC.md:245). references 합계는 +3,283자(직접 계산), L2 고유 로드는 49,759→50,955자(+2.4%)로 늘었다(N99:54).
- [추측] 직접 지시 과발동 같은 트리거 결함을 문구 추가로 고치기 어렵다. 고칠 때마다 다른 문구를 지우고 A6(트리거 평가)를 다시 돌려야 한다.

## 3. 우려스러운 점

**C1. 되돌리기 어려운 행동 게이트가 description 발동에만 의존 (중간; 범주 자체는 영향 큼)**
- [사실] 이전에는 매 요청 시작 시 본문(Step 0 포함)을 읽었다(5e833be:SKILL.md:3). M3에서 삭제 전 스킬 로드는 0/3이었고 r2·r3는 세션 내내 0회였지만, 승인 전 멈춤은 3/3이었다(RUN/nodes/N87.md:13).
- [사실] 외부 전송·저장소 밖 설정 변경·git 범주와 Codex 팀장은 측정하지 않았다(N99:119, :122). [추측] 멈춤이 스킬이 아니라 모델·호스트의 기본 신중함에서 나왔을 수 있어 다른 범주·호스트로 일반화하기 어렵다.

**C2. 수행 중 자동 전환 미작동 (중간)**
- [사실] description과 트리는 직접 처리 중 트리 2·3·5에 해당하면 적용으로 바꾸라고 하지만(SKILL.md:3, :27) 실측 자동 로드는 0이다(RUN/nodes/N88.md:29, N99:117). 사용자 확정 결정으로 수용된 한계다.
- [추측] 직접 처리로 오판된 요청이 로그 해석·광범위 탐색으로 번지면, 사용자가 전환을 지시하기 전까지 가장 긴 팀장 세션이 그 출력을 떠안는다. 토큰 목표와 역행한다.

**C3. 직접 수정 검토가 변경분이 아니라 결과만 본다 (중간)**
- [사실] 검토 입력에서 patch를 뺀다(assets/review-briefing.md:7, RUN/SPEC.md:43 설계대로). M5 r1 검토자는 "이전 버전과의 비교 … 수행하지 않음"이라고 적었다(RUN/final-live2/M5/r1/review-N1.md:16).
- [추측] git 추적 밖 대상이나 다른 미커밋 변경이 섞인 저장소에서는 고정 가이드 2(범위 밖 변경)·4(회귀)를 판정하기 어렵다.

**C4. 전환 뒤 검토 순서 준수 불안정 (중간)**
- [사실] 규칙은 "위임 실행·테스트 결과 뒤 검토"다(refs/level2.md:321). 엄격 순서가 확인된 통과는 M4 1/3뿐이고, r3 재시도 계획은 재실행 전에 검토를 배치했다(RUN/nodes/N88.md:5, :13).

**C5. Codex 경로 미검증 (중간)**
- [사실] A6 트리거 평가는 `claude -p`로만 했고(RUN/SPEC.md:249, N99:76), A8 팀장도 Claude였다(N99:119). 지원 대상은 Claude·Codex 둘이다(AGENTS.md:3).
- [추측] 적용 여부를 description만으로 정하게 된 만큼(SKILL.md:3), Codex의 스킬 선택 성향에 따라 D0 절감과 Step 0 멈춤이 달라질 수 있다.

**C6. 직접 처리 경로에서 본문 전용 금지 규칙이 빠짐 (낮음)**
- [사실] `Agent` 도구·`Workflow`·deep-research 금지는 본문에만 있고(SKILL.md:42) description에는 없다(SKILL.md:3). 이전에는 매 요청마다 본문을 읽어 늘 적용됐다(5e833be:SKILL.md:26).
- [추측] 직접 처리 중 팀장이 Claude `Agent`(Explore 등)로 탐색하면 Paseo 라우팅·기록 밖에서 위임이 생긴다.

**C7. 재독 생략에 따른 출력 형식 표류 (낮음)**
- [사실] 기동·완료 보고 직전의 FORM 재독 강제(5e833be:SKILL.md:425)가 "컨텍스트에 없으면 읽기"로 바뀌었다(SKILL.md:268, :272). [추측] 긴 세션에서 오래전에 읽은 견본에 기대면 출력 형식이 조금씩 어긋날 수 있다. 형식 준수 실측은 없다.

**C8. 효율 증거의 한계 (낮음~중간)**
- [사실] 전체 토큰/비용 절감률은 미입증이다(N99:8). A8 표의 Read 0은 네이티브 Read만 센 값인데, 확인한 팀장 세션 9개(N31, M4 5개, M5 3개 로그) 모두 셸 `sed -n`/`cat`으로 참조·자산을 읽었다(N99:100). 합의 27건 적용판의 L1 동적 재측정도 없다(N99:118). 따라서 I3의 동적 근거는 이전 판에서 한 N31 1회뿐이다.

**C9. 프리셋·문서 정합성 잔여 (낮음)**
- [사실] presets.md:188의 team-lead 권한 등급 "읽기·확인"(Codex는 가장 제약이 큰 `auto`; skills/profile-setup/references/provider-modes.md:56-57)과 presets.md:215의 "파일을 직접 고칠 이유가 없다"가 직접 처리·직접 수정 경로(SKILL.md:39-41)와 어긋난다. 이 PC의 실제 team-lead는 claude `bypassPermissions`라 지금은 영향이 없다(`~/.paseo/config.json` 조회).
- [사실] refs/brainstorming.md:29의 "「리더가 직접 하는 일」 4"는 번호 목록이 문장으로 바뀌어(SKILL.md:39) 가리킬 번호가 없다. 5e833be:SKILL.md:163 "순서를 바꿔 순차로 돌리는 것으로 레벨 2가 레벨 1이 되지는 않는다"는 통합으로 분류됐지만(RUN/rule-audit.md:117) 현재 문장(SKILL.md:151)에는 명시되지 않는다.
- [추측] spec notes 지침 "채택 이유·근거…는 빼고"(presets.md:240)를 스펙 워커가 판정안 파일에도 적용하면 판정안 필수 항목 "현재 근거"(refs/level2.md:139 절)와 충돌할 수 있다.

**C10. 직접 지시 과발동 4/15 (낮음, 기록된 한계)**
- [사실] 이전 993자판에서 측정했고 현재 1,010자판에서는 해당 질의를 측정하지 않았다(N99:116). 과발동하면 본문 18,175자가 불필요하게 로드된다.

## 수행 범위

- `git diff 5e833be 621e94b -- plugins/`와 `git show`로 12개 파일 대조, RUN/SPEC.md 전문, N99·N73·N87·N88 해당 절, M5 r1 검토 결과 확인.
- 팀장 세션 로그 9개(N31, M4 r1~r3·재시도 2, M5 r1~r3)에서 `graph_update`·`--help`·셸 참조 읽기·`command not found` 도구 호출을 python3로 파싱.
- 문자 수는 python3로 CRLF→LF 변환 뒤 `len`으로 계산. `~/.paseo/config.json`의 team-lead·review 필드는 읽기만 했고, `command -v python`/`python3`을 실행했다.
- 이 결과 파일 외에는 수정하지 않았다. RUN/nodes/N100.md는 읽지 않았다.

## 누락·미검증 항목 및 이유

- 이전 판본으로 같은 과제를 돌리는 동적 비교는 하지 않았다. 기존 비교군이 없고 이번 작업 범위 밖이다.
- self-test·A6·A8 재실행은 하지 않고 N99와 원시 로그를 인용했다.
- rule-audit 876행은 전수 재검토하지 않았다. 표본(5e833be:SKILL.md:163 행)만 확인했다.
- Codex·Windows 환경의 발동·`python` 명령 동작은 실행해 보지 않았다(C5·W3는 이 Mac과 기록 근거만 사용).
