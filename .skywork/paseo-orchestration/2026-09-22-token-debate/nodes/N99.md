판정: 대조 완료

# N99 — T18 최종 대조

## 결론

- **T18 대조 완료, 전체 수용 기준 통과 선언 불가.** A8-3은 최신 M3에서 1/3, A8-5는 최신 M4에서 통과 1·실패 1·승인 대기로 판정 불가 1이다. 스킬 동결에 따라 결함을 고치지 않았다. 현재 원본·설치본 SKILL.md SHA-256 앞 16자는 `8b4fc8139826703d`다(`nodes/N95.md:3-9`, `nodes/N96.md:5-7`).
- 효율 결론: **워커 생략·재독 감소 확인, 전체 토큰/비용 절감률 미입증**. 실패·미실행 경로는 **정적 개선, 실제 효율 미검증**이다. 전후 동적 비교군 없이 수치 차이를 절감률로 환산하지 않는다(`SPEC.md:272-273`, `nodes/N31.md:45`, `nodes/N88.md:72`).

## 수행 범위와 명령 증빙

결과 파일 작성 **전** 저장소 루트에서 실행했다. self-test만 스킬 디렉터리에서 실행했다. `sed`·`rg`·Python 읽기 전용 조회로 `SPEC.md`, `GRAPH.md`, 최신 노드와 원시 결과를 대조했다. 아래 `nodes/`·`a6-rerun4/`·`final-live/`·`final-live2/`·`a8-logs/`는 RUN 기준, `SKILL.md`·`references/level2.md`는 `plugins/paseo-toolkit/skills/agent-orchestration/` 기준, `presets.md`는 `plugins/paseo-toolkit/references/` 기준이다. 스테이징·커밋·푸시는 실행하지 않았다.

| 명령 | 종료 코드·핵심 출력 |
| --- | --- |
| `git status --short` | 0; 기존 추적 변경 8파일. N99 작성 전 신규 파일 0. |
| `git diff --check` | 0; 출력 없음. |
| `git diff 5e833be -- plugins/paseo-toolkit/.claude-plugin/plugin.json plugins/paseo-toolkit/.codex-plugin/plugin.json` | 0; 양쪽 `version`만 `0.11.0`→`0.12.0`. G7 사용자 승인 뒤 적용(`GRAPH.md:153-155`). |
| `python3 scripts/graph_update.py --self-test` | 0; `G1 통과`부터 `G11 통과`까지 11줄. Python 3.9 이전 결함은 N46에서 수정·재검증(`nodes/N46.md:3-22`). |
| self-test 전·후 `git status --porcelain=v1 --untracked-files=all`, `git diff --no-ext-diff` | 모두 0; 상태 출력 동일(해시 `b6647db7752d11f…`, 533바이트), diff 해시 동일(`792b763f5f65e6cf…`, 231,827바이트). self-test에 따른 저장소 변경 0. |
| `claude plugin validate plugins/paseo-toolkit` / `claude plugin validate .` | 각각 0; 각각 `Validation passed`. |
| `python3 RUN/link-audit.py --measure` / `python3 RUN/a6-rerun4/rule_audit_current.py` | 각각 0; `source errors: 0` / 이전 규칙 876·대조 행 876·누락 0·검사 오류 0. 전자는 출력 첫 두 줄만 확인. |
| Python 3 읽기 전용 해시·JSON·notes 대조 | 0; 현재 SKILL.md 18,175자·304줄·위 SHA-256, 두 plugin.json의 name/version/description 일치, Codex `skills=./skills/`, `presets.md`와 현재 `~/.paseo/config.json`의 두 notes 문자 일치. |

## 수용 기준별 판정

| 기준 | 판정과 근거 |
| --- | --- |
| A1 | **통과(정적).** 아래 시나리오 표; 본문 18,744→18,175자. 현재 측정 정본 `a6-rerun4/a1-a3-check.log:3`, `nodes/N96.md:23-36`. |
| A2 | **통과.** 그래프 갱신 지시는 `graph_update.py` 호출, 직접 편집 지시 0; L1 실측도 갱신 호출 확인(`nodes/N26.md:64-67`, `nodes/N31.md:11-12`, `SKILL.md:35`). |
| A3 | **통과.** 링크·경로·절·구조 오류 0, 본문 304줄, 정적 참조의 무조건 재독 지시 0(`nodes/N94.md:14-19`, `a6-rerun4/a1-a3-check.log:1-2`). |
| A4 | **통과(정적).** 규칙 단위·대조 행 각 876, 누락·검사 오류 0; 의도 변경·제거는 허용 범위이고 27개 축약 독립 검토 완료(`rule-audit.md`, `nodes/N27.md:3-11`, `nodes/N94.md:3-23`). |
| A5 | **통과.** 아래 G1~G11 전부 현재 Python 3.9에서 종료 0, 실행 전후 상태·diff 해시 동일(`SPEC.md:221-234`, 이 파일 명령 표). |
| A6 | **통과(정해진 28질의).** 동일 최신 해시로 정적 게이트 뒤 1차 28/28·재실행 16/16, 실패 경고 0. 세트·결과·stderr는 아래 표(`nodes/N96.md:5-22`). 직접 지시 근접 질의의 별도 과발동은 한계다. |
| A7 | **통과.** Claude 검증 두 건 0, 양쪽 plugin.json 일치·Codex skills 존재, `.claude-plugin/marketplace.json`과 `.agents/plugins/marketplace.json`에 `paseo-toolkit` 유지. 설치본 일치 뒤 N98에서 두 마켓플레이스 GitHub 출처도 복원됐다(`nodes/N94.md:20`, `nodes/N95.md:3-9`, `nodes/N98.md:3-12`, 현재 JSON 읽기 대조). |
| A8 | **실패.** A8-1 짧은 조회 3/3, A8-2 L1 1회 통과, A8-4 충돌 3/3. A8-3 M3 1/3, A8-5 M4 통과 1·실패 1·판정 불가 1. 현재 합의 27개 적용판의 A8 재실측은 없고, 특히 T15는 N31 뒤 재측정하지 않았다(`nodes/N73.md:5-11`, `nodes/N31.md:7-20`, `nodes/N74.md:5-13`, `nodes/N87.md:3-14`, `nodes/N88.md:3-13`, `nodes/N95.md:3-9`). |
| A9 | **한계.** G7에서 minor `0.12.0`과 설치 갱신 승인 후 두 버전을 같이 변경했고 N95 설치본이 일치한다(`GRAPH.md:153-155`, `nodes/N95.md:3-9`). 현재 두 notes는 `presets.md:214,240`과 디스크 설정이 문자 일치하며, 160자 초과 `spec` 206자 문구는 사용자 승인 축약안이다(`GRAPH.md:174`, `nodes/N34.md:3-19`). 현재 데몬의 `spec` notes를 이번 대조에서 재조회하지 않아 A9의 문자 일치 전체는 미확인이다. |
| C6 | **통과(적용 기록).** G6 승인, N34의 `list_profiles`는 team-lead·spec 두 notes 적용 및 다른 필드 불변을 확인했다. 현재 디스크 설정·presets 원문도 두 건 일치하고, 후속 M5의 team-lead 프로필에도 같은 notes가 있다. 현재 데몬 `spec` 재조회는 A9의 잔여 한계다(`GRAPH.md:145`, `nodes/N34.md:3-19`, `presets.md:214,240`, `final-live2/M5/team-lead-profile.json:10`). |
| C7 | **통과(전제 확인).** 현재 `~/.paseo/config.json`의 `daemon.mcp.injectIntoAgents=true`를 읽어 확인했다. 브리핑의 워커 재위임 금지는 본문에 있다(`SPEC.md:167`, `SKILL.md:33,213`). 워커별 도구 주입·재위임 방지의 일반성은 실측으로 입증하지 않았다(`nodes/N31.md:45`, `nodes/N54.md:39`). |
| C8 | **통과.** G7 버전 승인→설치 갱신→머지 전 A8 실측 순서. 현재 판본은 N95에서 재설치했으나 앞선 A8 결과를 그 해시로 재측정한 것은 아니다(`GRAPH.md:153-160`, `GRAPH.md:220-230`, `nodes/N95.md:3-9`). |
| R9 | **통과(정적 의미 대조), 실행 한계.** 의도 변경은 R1·R1-7·R2·R6·R7·R8에만, 나머지 876단위는 대조표에서 대응·인용 검사됐다. N94의 27개 축약 독립 리뷰도 의미 변화 0으로 판정했다. 실측 실패는 별도 한계로 둔다(`SPEC.md:153-157`, `nodes/N27.md:3-11`, `nodes/N94.md:3-23`). |

### A1 시나리오별 전후 측정

UTF-8·CRLF→LF·Python `len`; `로드`는 재독 포함/고유 합(자), `Read`는 스킬 본문 외 스킬 파일 읽기 호출이다. description **323→1,010자(+687)**, SKILL.md **18,744→18,175자(-569)**. 출처: `a6-rerun4/a1-a3-check.log:3`, `nodes/N96.md:23-36`.

| 시나리오 | 로드 전→후 (재독 포함 / 고유) | Read 전→후 | 워커 전→후 | 질문 묶음 전→후 | GRAPH 전→후 | 판정 |
| --- | --- | ---: | ---: | ---: | ---: | --- |
| D0a 위치 조회 | 29,002/29,002→0/0 | 7→0 | 1→0 | 0→0 | 1→0 | 통과 |
| D0b 간단 질문 | 29,002/29,002→0/0 | 7→0 | 1→0 | 0→0 | 1→0 | 통과 |
| D1 비경미 직접 수정 | 29,002/29,002→31,173/31,173 | 7→10 | 작업 1→0·검토 0→1 | 0→0 | 1→1 | 통과; Read 비교 조건 없음 |
| S0 삭제 승인 | 20,507/20,507→18,175/18,175 | 1→0 | 0→0 | 0→0 | 0→0 | 통과 |
| L1 독립 조사 3건 | 32,732/29,002→27,715/27,715 | 11→8 | 3→3 | 0→0 | 1→1 | 통과 |
| L2 개발 단계 | 58,054/49,759→50,955/50,955 | 20→19 | 6→4 | 3→1 | 1→1 | 통과; 고유 합은 증가 |

### A5·G1~G11

현재 Python 3.9의 `--self-test` stdout에 아래 각 항목 `통과`가 출력됐고 종료 코드 0이다. 오류·불변 경로는 임시 디렉터리에서 시험하므로 저장소 상태 해시도 불변이다(`SPEC.md:221-234`, `nodes/N46.md:3-22`).

| ID | 확인 내용 | 판정 | ID | 확인 내용 | 판정 |
| --- | --- | --- | --- | --- | --- |
| G1 | 기본·L1 견본 생성 | 통과 | G2 | 버전 우선·오류 진단 | 통과 |
| G3 | 기존 경로 생성 거부 | 통과 | G4 | 행·mermaid 추가 위치 | 통과 |
| G5 | 중복 ID·정의 누락 거부 | 통과 | G6 | 지정 셀만 변경 | 통과 |
| G7 | 잘못된 열·값·ID 거부 | 통과 | G8 | GRAPH 없음 거부 | 통과 |
| G9 | 한글·BOM·CRLF | 통과 | G10 | 실패 코드·stderr·대기 없음 | 통과 |
| G11 | 표준 라이브러리 import | 통과 | — | — | — |

### A6 평가 세트·결과·stderr

| 항목 | 판정·수치 | 근거 |
| --- | --- | --- |
| 대상·정적 게이트 | SHA `8b4fc8139826703d…`; description 1,010자, 21/21 인용, YAML·A1~A4 통과 | `nodes/N96.md:5-36`, `a6-rerun4/static-gate.json`, `a6-rerun4/static-check.json` |
| 1차 세트·결과 | 발동 18(트리 1~5·독립 검토·되돌리기·브레인스토밍), 비발동 10(위치·이름 나온 파일·종료 코드·간단 질문); 28/28 통과, failed 0 | `a6-rerun4/initial-eval.json`, `a6-rerun4/initial-result.json:518`, `nodes/N96.md:74-105` |
| 재실행 선별·결과 | 사전 boundary 16건, 불일치·경고 0; 질의당 3회, 16/16 통과, failed 0 | `a6-rerun4/rerun-selection.json`, `a6-rerun4/rerun-eval.json`, `a6-rerun4/rerun-result.json:398`, `nodes/N96.md:109-133` |
| stderr·실행 환경 | `initial-stderr.log`·`rerun-stderr.log` 각 0바이트, `Warning: query failed` 0; Claude CLI 2.1.281, `claude-opus-5-5`, 76/76 격리 폴더 정리 | `a6-rerun4/initial-stderr.log`, `a6-rerun4/rerun-stderr.log`, `nodes/N96.md:18-22` |

### A8 실측: 팀장+모든 워커의 세션 누적 수치

각 행 `입력/캐시입력/출력`은 결과 파일에 적힌 `inputTokens/cachedInputTokens/outputTokens`, 뒤 값은 모델 호출/워커/Read다. M4의 r2·r3 원시도와 허용된 재시도도 빠짐없이 싣는다. A8-1·4는 N73·N74, A8-2는 N31, A8-3은 N87, A8-5는 N88이 GRAPH 기준 최신 노드다(`GRAPH.md:150-160`, `GRAPH.md:207-225`).

| 기준·회 | 판정 | 입력 / 캐시입력 / 출력 | 호출 / 워커 / Read | 수치 근거 |
| --- | --- | ---: | ---: | --- |
| A8-1 r1 | 통과 | 55,738 / 39,576 / 348 | 2 / 0 / 0 | `nodes/N73.md:19-26`, `final-live/M1/r1/metrics.json` |
| A8-1 r2 | 통과 | 55,735 / 39,574 / 287 | 2 / 0 / 0 | `nodes/N73.md:19-26`, `final-live/M1/r2/metrics.json` |
| A8-1 r3 | 통과 | 54,081 / 37,747 / 483 | 2 / 0 / 0 | `nodes/N73.md:19-26`, `final-live/M1/r3/metrics.json` |
| A8-2 N31 1회 | 통과·현재판 재측정 없음 | 2,107,735 / 1,974,909 / 22,244 | 48 / 3 / 23* | `nodes/N31.md:22-34`, `a8-logs/N31/metrics.json` |
| A8-3 r1 | 통과 | 206,342 / 171,988 / 2,045 | 6 / 0 / 0 | `nodes/N87.md:18-28`, `final-live2/M3/r1/metrics.json` |
| A8-3 r2 | 실패: 생략 미보고 | 145,122 / 126,976 / 1,882 | 5 / 0 / 0 | `nodes/N87.md:18-28`, `final-live2/M3/r2/metrics.json` |
| A8-3 r3 | 실패: 생략 미보고 | 144,843 / 126,705 / 1,905 | 5 / 0 / 0 | `nodes/N87.md:18-28`, `final-live2/M3/r3/metrics.json` |
| A8-4 r1 | 통과 | 27,788 / 11,824 / 1,242 | 1 / 0 / 0 | `nodes/N74.md:19-26`, `final-live/M2/r1/metrics.json` |
| A8-4 r2 | 통과 | 71,910 / 39,612 / 1,257 | 2 / 0 / 0 | `nodes/N74.md:19-26`, `final-live/M2/r2/metrics.json` |
| A8-4 r3 | 통과 | 27,788 / 11,824 / 971 | 1 / 0 / 0 | `nodes/N74.md:19-26`, `final-live/M2/r3/metrics.json` |
| A8-5 r1 | 실패: 검토 0 | 1,063,672 / 985,360 / 11,746 | 25 / 1 / 0 | `nodes/N88.md:37-55`, `final-live2/M4/r1/metrics.json` |
| A8-5 r2 원시도 | 판정 불가: 승인 대기 | 1,181,426 / 1,100,958 / 13,003 | 22 / 1 / 0 | `nodes/N88.md:37-55`, `final-live2/M4/r2/metrics.json` |
| A8-5 r2 재시도 | 통과 | 2,134,706 / 2,023,187 / 28,293 | 50 / 3 / 0 | `nodes/N88.md:37-55`, `final-live2/M4/r2/retry1/metrics.json` |
| A8-5 r3 원시도 | 판정 불가: 승인 대기 | 1,099,939 / 1,025,265 / 12,725 | 22 / 1 / 0 | `nodes/N88.md:37-55`, `final-live2/M4/r3/metrics.json` |
| A8-5 r3 재시도 | 판정 불가: 승인 대기 | 1,069,957 / 990,012 / 12,108 | 22 / 1 / 0 | `nodes/N88.md:37-55`, `final-live2/M4/r3/retry1/metrics.json` |

\* N31의 Read 23은 셸 안의 내용 읽기까지 센 값이다. 네이티브 `Read` 도구만 세면 0이다. 다른 행의 Read 0은 네이티브 도구 기준이며 셸 읽기 0을 뜻하지 않는다(`nodes/N31.md:34-35`, `nodes/N87.md:30`, `nodes/N88.md:33-35`).

동작 대조: N73은 세 위치 답이 fixture와 일치하고 위임 0, N31은 `create_agent` 3회·그래프 스크립트 갱신·같은 참조 절 재독 0·결과 파일 3개 합격, N74는 질문 뒤 무응답 중 파일·그래프·워커 변경 0이다. N87은 세 회 모두 승인 전 잠금 파일 해시 유지·프로필 조회/워커/그래프/삭제 0 및 승인 후 적용 성공이다. N88은 모든 시도에서 첫 수정 뒤 전환 지시 다음 첫 도구가 Skill이고 전환 전 수정이 보존됐다. 최종 검토 충족은 r2 재시도뿐이다(`nodes/N73.md:5-11`, `nodes/N31.md:7-20`, `nodes/N74.md:5-13`, `nodes/N87.md:5-17`, `nodes/N88.md:7-30`).

추가 직접 수정 검토 M5(현행 N89)는 3/3 통과했다. r1/r2/r3의 누적 입력은 각각 **994,700 / 993,611 / 1,128,606**, 캐시 입력 **915,499 / 910,384 / 1,045,580**, 출력 **11,101 / 11,595 / 11,135**, 호출 **22 / 22 / 24**, 워커 **각 1**, Read **각 0**이다(`nodes/N89.md:5-16`, `nodes/N89.md:20-37`, `final-live2/M5/r1/metrics.json`, `r2/metrics.json`, `r3/metrics.json`).

이력 한 줄: 이전 N54의 A8-1·4 단발 통과, N75의 M3 2/3(성공 두 회 입력 1,250,746·1,031,286), N76의 M4 2/3, N77의 M5 2/3, N86의 A1 크기 게이트 실패 뒤 N96 재평가 통과, N97 복원 실패 뒤 G19 승인·N98 복원 완료(`nodes/N54.md:5-15`, `nodes/N75.md:5-18`, `nodes/N76.md:3-14`, `nodes/N77.md:3-17`, `nodes/N86.failure.md:1-5`, `nodes/N96.md:5-22`, `nodes/N97.failure.md:1-7`, `nodes/N98.md:3-12`). M3 이전 성공 두 회는 각각 약 100만 입력 토큰, 현행 N87 세 회 평균은 165,435.7이다. 완료 여부·워커 수가 달라 전체 절감률로 해석하지 않는다(`nodes/N75.md:24-32`, `nodes/N87.md:18-28`).

## 남은 미검증·한계와 후속

| 구분 | 내용·근거 |
| --- | --- |
| 한계 | 경미한 수정 판정을 스킬 본문을 읽지 않고 한 M3 r2·r3에서 완료 보고의 **검토 생략 사실이 2/3회 누락**됐다. 세 회 모두 한 파일 기존 설정값 한 줄과 실행 확인은 충족했지만 전체 경로는 1/3만 통과(`nodes/N87.md:3-14`, `nodes/N87.md:16-17`, `SKILL.md:41`). |
| 한계 | M4 r1에서 두 줄 수정인데 한 줄 값 변경으로 예외를 넓게 적용해 독립 검토가 0건이었다(`nodes/N88.md:5-9`, `final-live2/M4/r1/a-change.diff:7-8`, `final-live2/M4/r1/leader.log:60`). |
| 한계 | 전환 뒤 엄격한 **위임 실행·테스트 완료→검토** 순서의 확인된 통과는 M4 1/3뿐이다. r3은 승인 대기라 실행 판정이 불가하고 제시 계획도 순서 불일치 가능성이 있다(`nodes/N88.md:5-13`, `plugins/paseo-toolkit/skills/agent-orchestration/references/level2.md:321`). G14의 합류 기준은 N72 지적 뒤 철회되어 순차 규칙이 유지됐다(`GRAPH.md:204-213`). |
| 한계 | M4 r2·r3은 첫 실행 실패 뒤 계획 승인 질문에서 멈췄다. 답을 주지 않는 실측 방식에서는 후속 수정·재실행·검토를 판정할 수 없다(`nodes/N88.md:10-13`, `nodes/N88.md:72`). |
| 한계 | **직접 지시 과발동 4/15**: 이전 993자판의 같은 q30이 N69에서 3/5, N70 CUR에서 1/10 발동했다. 현재 1,010자판의 A6 28질의에는 이 근접 q30이 없어 최신 판본의 해당 비발동률은 미검증이다(`nodes/N69.md:3-21`, `nodes/N70.md:3-17`, `nodes/N96.md:74-105`). |
| 미검증 | **트리 3·5 전환 실측 미검증; 작업 도중 자동 전환 미보장(사용자 지시 전환으로 대체).** M4 전환 지시 뒤 첫 도구 Skill은 모든 시도에서 관찰됐으나 지시 전 자동 로드는 0건이다(`nodes/N88.md:27-29`, `nodes/N88.md:69-72`). |
| 미검증 | **T15 미재측정.** N31은 3워커·그래프·재독 0을 확인했으나 N46의 Python 3.9 수정과 N95의 합의판 설치 이후 동적 팬아웃 재측정은 없다. 최신판은 정적 개선, 실제 효율 미검증(`nodes/N31.md:7-20`, `nodes/N31.md:35-45`, `nodes/N46.md:3-22`, `nodes/N95.md:3-9`). |
| 미검증 | **PowerShell 경로 미검증.** macOS 실측은 `apply-port.ps1`·`Get-FileHash` 대신 셸 스크립트·SHA-256을 사용했다(`nodes/N87.md:37`, `MILESTONE01-Tasks.md:165-172`). Codex가 팀장일 때 수행 중 삭제 전 멈춤도 보장 증거가 없다. M3 대상은 Claude 팀장이었다(`nodes/N87.md:29-31`). |
| 후속 | `plugins/paseo-toolkit/skills/profile-setup/SKILL.md:204`의 “notes가 워커에 전달되지 않는다” 설명은 현행 브리핑의 notes 원문 전달(`SKILL.md:213`)과 맞지 않는다. 별도 범위에서 정정 필요. |
| 후속 | `plugins/paseo-toolkit/references/presets.md:215`의 team-lead 권한 근거 “파일을 직접 고칠 이유가 없다”는 직접 처리·직접 수정 경로(`SKILL.md:39-41`)와 맞지 않는다. 별도 범위에서 정정 필요. |
| 후속 | Codex 팀장 Step 0 삭제 전 중단 경로는 별도 실측이 필요하다. 현재 M3 실측은 Claude에만 해당한다(`nodes/N87.md:29-31`, `SPEC.md:267-268`). |
| 후속 | 효율 보고는 **워커 생략·재독 감소 확인, 전체 토큰/비용 절감률 미입증**으로 제한한다. 비교군·가격 가중 비용이 없고 실패/미실행 부분은 **정적 개선, 실제 효율 미검증**이다(`SPEC.md:272-273`, `nodes/N31.md:45`). |
| 후속 | 기존 잔여 fixture 워크스페이스·디렉터리는 대상을 다시 식별하고 **사용자 확인 후** 정리한다. N31의 workspace는 archive됐지만 fixture 디렉터리는 보존됐다. 다른 실측 workspace는 각 노드에 archive 기록이 있어 현재 잔여 목록은 이번 대조에서 확정하지 않았다(`nodes/N31.md:42`, `nodes/N87.md:29-33`, `nodes/N88.md:68-70`). |

Git 커밋은 별도 사용자 승인 뒤 단계다. 이번 작업에서 실행하지 않았다(`AGENTS.md:122`, `MILESTONE01-Tasks.md:188`).
