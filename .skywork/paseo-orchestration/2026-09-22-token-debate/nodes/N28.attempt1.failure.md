# N28 실패 — T12 description 고정과 트리거 평가

## 결론

**A6·C3 실패.** `description`의 정적 조건과 평가 세트 구성은 통과했지만, 최종 트리거 평가는 재실행 22개 중 14개 불일치(`summary.failed = 14`)로 합격 기준을 충족하지 못했다. `description`은 고정 뒤 다시 수정하지 않았다.

## 수행 범위와 정적 검증

- `plugins/paseo-toolkit/skills/agent-orchestration/SKILL.md:3`의 frontmatter `description`만 수정했다. 1,042자에서 **1,021자**로 줄였고 `<`·`>` 0개, YAML 파싱 성공, CRLF 304개, BOM 없음, CRLF→LF 기준 18,610자, 304줄이다.
- `RUN/SPEC.md:18`의 R1-2 트리 머리·여섯 항목·네 줄 문장 12개 줄 조각을 공백 정규화 뒤 원문·순서대로 대조했다. A6의 나머지 여덟 항목도 스크립트로 각각 대조했다. 수정 후 정적 검사 종료 코드 0: `POST_STATIC_OK description=1021 tree_segments=12 A6_items=8 eval=26 trigger=16 nontrigger=10 near_nontrigger=8 required_queries=10`.
- 스파이크 `RUN/nodes/N12.md`에서 `이 에러 원인 찾아줘`의 파일 목록 조회가 선행됐으므로, 이 짧은 질의가 첫 도구 호출 전에 적용된다는 문장을 트리 원문 밖에 추가했다. 중복된 직접 처리 예외 문장을 줄여 길이 제한을 맞췄다. 아래 측정에서 이 보강만으로 첫 도구 호출 전 발동을 확보하지 못했다.
- `RUN/a6-eval.json`: 발동 16개(트리 1~5, 독립 검토, 되돌리기 행동, 브레인스토밍 각 2개), 비발동 10개(검색식 한 번 2개, 이름 나온 5개 이하 파일 3개, 종료 코드·통과 여부 2개, 간단한 질문 2개, 직접 지시 1개). 비발동 근접 사례 8개는 모두 `boundary: true`이고 필수 질의 10개가 포함돼 있다.
- `python .skywork/paseo-orchestration/2026-09-22-token-debate/link-audit.py` 종료 코드 0, `source errors: 0`. 스킬 디렉터리에서 `python scripts/graph_update.py --self-test` 종료 코드 0, G1~G11 모두 통과. `claude.cmd auth status` 종료 코드 0, `loggedIn: true`.

## 평가 실행과 판정

- 처음에는 `RUN/MILESTONE01-Tasks.md:138`의 원문 PowerShell 블록이 프로세스 생성 전에 `CreateProcess ... Rejected(... blocked by policy)`로 거부됐다. 팀장의 추가 지시에 따라 `finally`의 `Remove-Item -LiteralPath $work -Recurse -Force` 한 줄만 제외했다.
- 삭제만 제외한 첫 실행은 Python이 평가 세트의 UTF-8 한글을 cp949로 읽어 `UnicodeDecodeError`로 종료했다. `a6-result.json`은 0바이트, `a6-stderr.log`에 traceback이 남았었다. 평가 블록 밖 실행 환경에 `PYTHONUTF8=1`을 설정하고 동일한 평가 블록을 다시 실행했다. 평가 사본과 `--model` 생략, `--runs-per-query 1/3`, `--trigger-threshold 0.5`, 출력 경로는 유지했다.
- 유효한 1차 실행: `python -m scripts.run_eval --eval-set <a6-eval.json 절대 경로> --skill-path <스킬 디렉터리 절대 경로> --runs-per-query 1 --trigger-threshold 0.5 @modelArgs > <a6-result.json> 2> <a6-stderr.log>`, 명령 블록 종료 코드 0, `summary = {total: 26, passed: 12, failed: 14}`. stderr는 0바이트이고 `Warning: query failed`는 없었다.
- 1차 불일치 14개와 미리 `boundary: true`인 13개의 합집합 **22개**만 `RUN/a6-rerun-eval.json`에 저장했다. 재실행 대상이 아닌 4개는 모두 1차 기대값과 일치했다.
- 3회 재실행: 같은 명령에서 `--eval-set`·출력 경로를 `a6-rerun-*`로, `--runs-per-query`만 `3`으로 설정했다. 명령 블록 종료 코드 0, `summary = {total: 22, passed: 8, failed: 14}`. stderr는 0바이트다. 결과 대조 스크립트 종료 코드 0: `RESULT_AUDIT_OK initial=12/26 rerun=8/22 rerun_failed=14 unrerun_passed=4 stderr_empty=2`.
- `a6-result.json`과 `a6-rerun-result.json`에는 사용 모델 필드가 없고 양쪽 stderr도 비어 있다. 실제 사용 모델은 **확인 불가**다.

## 실패 질의 — 기대값과 실제값

아래 14개는 모두 발동 기대 질의이며, 재실행에서 필요한 2/3 발동에 미달했다. 비발동 기대 질의는 1차와 재실행 대상의 3회 판정을 모두 통과했다.

| 질의 | 기대값 | 1차 실제 | 3회 재실행 실제 |
| --- | --- | --- | --- |
| 설정값이 어디서 바뀌는지 관련 파일을 찾아 읽고 알려줘 | 발동 | 0/1 발동 | 0/3 발동 |
| 설정 오류 조사 작업은 워커에게 맡겨줘 | 발동 | 0/1 발동 | 1/3 발동 |
| 이 문서 정리 작업을 담당 워커에게 배정해줘 | 발동 | 0/1 발동 | 0/3 발동 |
| 이 에러 원인 찾아줘 | 발동 | 0/1 발동 | 0/3 발동 |
| `test.log` 전체를 읽고 실패 원인을 분석해줘 | 발동 | 0/1 발동 | 1/3 발동 |
| docs/ 아래 30개 파일 링크 통일해줘 | 발동 | 0/1 발동 | 0/3 발동 |
| `a.py`, `b.py`, `c.py`, `d.py`, `e.py`, `f.py`의 헤더를 같은 형식으로 고쳐줘 | 발동 | 0/1 발동 | 1/3 발동 |
| `cache.py`에 만료 정책을 설계하고 구현해줘 | 발동 | 0/1 발동 | 0/3 발동 |
| `a.py` 고치고 리뷰 에이전트한테 독립 검토 받아줘 | 발동 | 0/1 발동 | 0/3 발동 |
| `auth.py`의 새 정책 설계안을 작성하고, 구현한 다음 테스트 결과도 정리해줘 | 발동 | 0/1 발동 | 0/3 발동 |
| `b.py` 변경안을 다른 에이전트에게 독립 검토 맡겨줘 | 발동 | 0/1 발동 | 0/3 발동 |
| 네가 직접 build 폴더 지워줘 | 발동 | 0/1 발동 | 1/3 발동 |
| 네가 직접 브레인스토밍하자 | 발동 | 0/1 발동 | 1/3 발동 |
| 이 기능 아이디어 좀 내보자 | 발동 | 0/1 발동 | 0/3 발동 |

가능한 공통 원인: 평가 사본은 첫 도구 호출만 판정하므로 스킬보다 다른 도구가 먼저 호출됐거나, 1,021자 `description`이 초기 스킬 선택을 충분히 유도하지 못했을 수 있다. 결과 JSON은 불리언 발동 여부만 담아 각 질의의 실제 첫 도구와 정확한 원인을 특정할 수 없다. 스파이크에서 약한 발동을 보인 짧은 오류 원인 질의도 재실행 0/3이었다. 이 원인은 **추정**이며, 합격할 때까지 `description`을 반복 수정하지 말라는 지시에 따라 추가 수정·평가를 하지 않았다.

## 보존 파일과 남은 임시 폴더

- 평가 세트·결과·stderr: `RUN/a6-eval.json`, `RUN/a6-result.json`, `RUN/a6-stderr.log`, `RUN/a6-rerun-eval.json`, `RUN/a6-rerun-result.json`, `RUN/a6-rerun-stderr.log`. 유효한 실행의 stderr 두 파일은 0바이트다.
- 첫 인코딩 오류 시도: `C:\Users\magedoga\AppData\Local\Temp\skill-trigger-eval-f50acfad8f1d431db43f57677e405f54`.
- 유효한 1차 실행: `C:\Users\magedoga\AppData\Local\Temp\skill-trigger-eval-6b008cb13ea747769a249e3e020d2f61`.
- 3회 재실행: `C:\Users\magedoga\AppData\Local\Temp\skill-trigger-eval-4456bd1ef98449d797155a7c8c889ff2`.
- 세 임시 폴더는 사용자 승인 절차에 따라 워커가 삭제하지 않았다. 평가 결과는 위 RUN 파일에 저장돼 있고 JSON 파싱과 질의 집합 대조를 마쳤다. `RUN/nodes/N28.md`는 합격 보고가 아니므로 만들지 않았다.

## 미완료 범위

A6의 발동 합격과 C3 트리거 평가 합격이 미완료다. 직접 수정 뒤 후발동과 직접·워커 지시 충돌은 T12 평가 세트 밖의 후속 실측 항목이라 이번 실행에서 측정하지 않았다. `RUN/SPEC.md`·`RUN/MILESTONE01-Tasks.md`와 두 `plugin.json`은 수정하지 않았고 Git 스테이징·커밋·푸시도 하지 않았다.
