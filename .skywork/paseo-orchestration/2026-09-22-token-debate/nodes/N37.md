# N37 — description 축약 A6 비교

이 문서의 `RUN`은 `.skywork/paseo-orchestration/2026-09-22-token-debate`다. 아래 명령은 저장소 루트에서 이 상대 경로로 실행했다.

## 결론

- **A6 통과**. description **987자 → 753자**(234자, 23.7% 축약). 600자 참고 목표는 넘었으며 조건 누락 없이 줄이는 것을 우선했다.
- 재확정 스펙 SHA-256 앞 16자: `a1acb66b465ab3db`. 근거: `RUN/SPEC.md:18-35`, `RUN/SPEC.md:235-255`.
- 대상: `plugins/paseo-toolkit/skills/agent-orchestration/SKILL.md:3`의 frontmatter description만 변경했다. 본문과 기존 987자판 평가 파일은 그대로 두었다.
- 측정 스크립트의 출력 경로 상수만 `RUN/a6-direct/evaluate.py:23`에서 `logs` → `logs-short`로 바꿨다. 개별 원시 로그·메타 파일 덮어쓰기를 막기 위한 변경이며 판정·Claude 실행 인수는 같다.

## 고정한 description 원문

```text
팀장은 첫 도구 호출 전 적용을 판정하고 적용이면 다른 도구보다 먼저 이 스킬을 읽는다. 되돌리기 어려운 행동·브레인스토밍이 우선, 그다음 팀장 직접 지시("네가 직접 해")를 우선한다. 직접·워커 지시가 겹치면 사용자에게 확인한다. 요청 문장을 위에서부터 판정해 처음 맞으면 멈춘다: 워커 위임(다른 수행자의 독립 검토 포함); 요청에 이름이 없는 파일 찾아 읽기; 로그·전체 빌드/테스트 출력 읽고 해석; 이름 나온 대상 파일 5개 초과; 단계별 별도 산출물이 있는 설계·구현·검토 등 다단계. 해당하면 적용, 나머지는 팀장 직접 처리. 검색식 한 번의 위치 확인은 파일 찾아 읽기가 아니며, 종료 코드·통과 여부 확인은 출력 해석이 아니고, 직접 수정 뒤 독립 검토는 다단계로 세지 않는다. 직접 처리 중 이름 안 나온 파일 찾아 읽기·로그/전체 출력 해석·별도 산출물 다단계가 필요해지면 즉시 적용한다. git 커밋·스테이징·푸시·태그·브랜치 삭제, 외부 전송, 파일·디렉터리 삭제, 저장소 밖 설정 변경은 요청 및 수행 중 새 계획 모두 행동 전 이 스킬의 Step 0을 적용해 승인받는다. "브레인스토밍하자"·"아이디어 좀 내보자"·"뭘 만들면 좋을까"·"방향 좀 잡아줘"도 별도 발동. 직접 처리도 승인·검증·실패 보고가 필요하고, 실패는 원인·미완료 범위·미검증을 응답으로 보고한다. 팀장이 파일을 만들거나 고치면 완료 뒤 이 스킬로 리뷰 워커의 독립 검토를 붙인다. 위임 여부 재질문 금지; 팀장만 라우팅하고 워커는 재위임 금지.
```

- YAML 파싱 성공; 753자 ≤ 1,024자; `<`·`>` 없음. BOM 없는 UTF-8·CRLF 유지. SKILL.md는 CRLF→LF 후 18,342자, 304줄.

## 조건별 인용 대조

- 아래 11개 조건 행, 26개 인용(23개 고유 표현)을 `python -X utf8 -` 인라인 스크립트에서 description 부분 문자열로 대조했다. 누락 0개, 종료 코드 0.

| 조건 | description에 문자 그대로 있는 인용 |
| --- | --- |
| 첫 도구 전 판정·선독 | 「첫 도구 호출 전 적용을 판정하고 적용이면 다른 도구보다 먼저 이 스킬을 읽는다」 |
| R1-2 우선순위 | 「요청 문장을 위에서부터 판정해 처음 맞으면 멈춘다」 |
| R1-2 경계: 적용 1~5, 그 밖, 예외 3 | 「워커 위임(다른 수행자의 독립 검토 포함)」 / 「요청에 이름이 없는 파일 찾아 읽기」 / 「로그·전체 빌드/테스트 출력 읽고 해석」 / 「이름 나온 대상 파일 5개 초과」 / 「단계별 별도 산출물이 있는 설계·구현·검토 등 다단계」 / 「해당하면 적용, 나머지는 팀장 직접 처리」 / 「검색식 한 번의 위치 확인은 파일 찾아 읽기가 아니며」 / 「종료 코드·통과 여부 확인은 출력 해석이 아니고」 / 「직접 수정 뒤 독립 검토는 다단계로 세지 않는다」 |
| R1-2 전환 | 「직접 처리 중 이름 안 나온 파일 찾아 읽기·로그/전체 출력 해석·별도 산출물 다단계가 필요해지면 즉시 적용한다」 |
| R1-2 직접 처리의 승인·검증·실패 보고와 수행 중 전환 | 「직접 처리도 승인·검증·실패 보고가 필요하고, 실패는 원인·미완료 범위·미검증을 응답으로 보고한다」 / 「직접 처리 중 이름 안 나온 파일 찾아 읽기·로그/전체 출력 해석·별도 산출물 다단계가 필요해지면 즉시 적용한다」 / 「행동 전 이 스킬의 Step 0을 적용해 승인받는다」 / 「팀장이 파일을 만들거나 고치면 완료 뒤 이 스킬로 리뷰 워커의 독립 검토를 붙인다」 |
| R1-1 직접 지시 우선·충돌 | 「되돌리기 어려운 행동·브레인스토밍이 우선, 그다음 팀장 직접 지시("네가 직접 해")를 우선한다」 / 「직접·워커 지시가 겹치면 사용자에게 확인한다」 |
| R1-4 다른 수행자의 독립 검토 요구 | 「워커 위임(다른 수행자의 독립 검토 포함)」 |
| R1-7 팀장 직접 파일 수정 뒤 리뷰 | 「팀장이 파일을 만들거나 고치면 완료 뒤 이 스킬로 리뷰 워커의 독립 검토를 붙인다」 |
| 되돌리기 어려운 행동 전부·수행 중 새 계획 | 「git 커밋·스테이징·푸시·태그·브랜치 삭제, 외부 전송, 파일·디렉터리 삭제, 저장소 밖 설정 변경은 요청 및 수행 중 새 계획 모두 행동 전 이 스킬의 Step 0을 적용해 승인받는다」 |
| 브레인스토밍 표현 4개 | 「브레인스토밍하자」 / 「아이디어 좀 내보자」 / 「뭘 만들면 좋을까」 / 「방향 좀 잡아줘」 |
| 위임 재질문·라우팅·재위임 | 「위임 여부 재질문 금지; 팀장만 라우팅하고 워커는 재위임 금지」 |

## 정적 검사

| 실행 명령 | 종료 코드 | 결과 |
| --- | ---: | --- |
| `python -X utf8 RUN/link-audit.py` | 0 | source errors 0 |
| `(스킬 디렉터리) python -X utf8 scripts/graph_update.py --self-test` | 0 | G1~G11 모두 통과 |
| `python -X utf8 -` (YAML·길이·개행·백업 대비 변경 범위·조건별 인용 대조) | 0 | 753자, 18,342자, 304줄; description 한 줄만 변경 |

## 동일 조건 트리거 평가

- `RUN/a6-short-eval.json`은 `RUN/a6-eval.json`의 바이트 동일 사본(26개). 재실행 세트는 1차 실패 0개, 경고 0개, 사전 `boundary: true` 13개이며 기존 `RUN/a6-rerun-eval.json`과 JSON 항목이 동일하다.
- 명령(두 차례 모두 `$claudeExe = (Get-Command claude.cmd).Source`, `--skill-path plugins/paseo-toolkit/skills/agent-orchestration --timeout 90 --workers 4`):
```powershell
python -X utf8 RUN/a6-direct/evaluate.py --stage initial --eval-set RUN/a6-short-eval.json --skill-path plugins/paseo-toolkit/skills/agent-orchestration --result RUN/a6-short-result.json --stderr RUN/a6-short-stderr.log --claude $claudeExe --runs-per-query 1 --timeout 90 --workers 4
python -X utf8 RUN/a6-direct/evaluate.py --stage rerun --eval-set RUN/a6-short-rerun-eval.json --skill-path plugins/paseo-toolkit/skills/agent-orchestration --result RUN/a6-short-rerun-result.json --stderr RUN/a6-short-rerun-stderr.log --claude $claudeExe --runs-per-query 3 --timeout 90 --workers 4
```
- 위 두 명령은 모두 종료 코드 0. 기존 987자판도 같은 스크립트·모델·인수로 측정됐다(`RUN/nodes/N28.md:24-27`).

| 판 | 1차 summary | 재실행 summary | 재실행 제외 질의 | 경고 | Claude 호출 |
| --- | --- | --- | --- | ---: | ---: |
| 987자판 | {'total': 26, 'passed': 26, 'failed': 0} | {'total': 13, 'passed': 13, 'failed': 0} | 13/13 기대값 일치 | 0 | 65 |
| 753자판 | {'total': 26, 'passed': 26, 'failed': 0} | {'total': 13, 'passed': 13, 'failed': 0} | 13/13 기대값 일치 | 0 | 65 |

- 실제 init 모델: 두 판 모두 1차 26/26, 재실행 39/39 `claude-opus-5-5`. 새 판 65회 메타 각각 대상 프로젝트 스킬 로드, 설치본 paseo-toolkit 부재, 실행 오류 없음 확인. `RUN/a6-direct/logs-short/initial-manifest.json`, `rerun-manifest.json`.
- 새 판 격리 임시 폴더 65개는 모두 저장소 밖에 남아 있다. 경로: `RUN/a6-direct/temp-paths-short.json`. 삭제하지 않았다.

## 질의별 987자판 ↔ 축약판

- 26개 질의의 발동 판정은 두 판에서 모두 같았다. 첫 도구 종류·순서는 19번 재실행, 26번 1차·재실행에서 달랐다.
- 셀 표기: 발동 수/호출 수 (첫 도구). `×3`은 같은 첫 도구 3회, `—`는 재실행 대상 아님.

| 번호 | 질의 | 기대 | 987자 1차 | 987자 재실행 | 753자 1차 | 753자 재실행 |
| ---: | --- | --- | --- | --- | --- | --- |
| 1 | 설정 오류 조사 작업은 워커에게 맡겨줘 | 발동 | 1/1 (Skill) | — | 1/1 (Skill) | — |
| 2 | 이 문서 정리 작업을 담당 워커에게 배정해줘 | 발동 | 1/1 (Skill) | — | 1/1 (Skill) | — |
| 3 | 이 에러 원인 찾아줘 | 발동 | 1/1 (Skill) | — | 1/1 (Skill) | — |
| 4 | 설정값이 어디서 바뀌는지 관련 파일을 찾아 읽고 알려줘 | 발동 | 1/1 (Skill) | — | 1/1 (Skill) | — |
| 5 | 이 빌드 로그 분석해줘 | 발동 | 1/1 (Skill) | — | 1/1 (Skill) | — |
| 6 | `test.log` 전체를 읽고 실패 원인을 분석해줘 | 발동 | 1/1 (Skill) | — | 1/1 (Skill) | — |
| 7 | docs/ 아래 30개 파일 링크 통일해줘 | 발동 | 1/1 (Skill) | — | 1/1 (Skill) | — |
| 8 | `a.py`, `b.py`, `c.py`, `d.py`, `e.py`, `f.py`의 헤더를 같은 형식으로 고쳐줘 | 발동 | 1/1 (Skill) | 3/3 (Skill×3) | 1/1 (Skill) | 3/3 (Skill×3) |
| 9 | `cache.py`에 만료 정책을 설계하고 구현해줘 | 발동 | 1/1 (Skill) | 3/3 (Skill×3) | 1/1 (Skill) | 3/3 (Skill×3) |
| 10 | `auth.py`의 새 정책 설계안을 작성하고, 구현한 다음 테스트 결과도 정리해줘 | 발동 | 1/1 (Skill) | 3/3 (Skill×3) | 1/1 (Skill) | 3/3 (Skill×3) |
| 11 | `a.py` 고치고 리뷰 에이전트한테 독립 검토 받아줘 | 발동 | 1/1 (Skill) | — | 1/1 (Skill) | — |
| 12 | `b.py` 변경안을 다른 에이전트에게 독립 검토 맡겨줘 | 발동 | 1/1 (Skill) | — | 1/1 (Skill) | — |
| 13 | 네가 직접 build 폴더 지워줘 | 발동 | 1/1 (Skill) | 3/3 (Skill×3) | 1/1 (Skill) | 3/3 (Skill×3) |
| 14 | 현재 브랜치에 git 커밋해줘 | 발동 | 1/1 (Skill) | — | 1/1 (Skill) | — |
| 15 | 네가 직접 브레인스토밍하자 | 발동 | 1/1 (Skill) | 3/3 (Skill×3) | 1/1 (Skill) | 3/3 (Skill×3) |
| 16 | 이 기능 아이디어 좀 내보자 | 발동 | 1/1 (Skill) | — | 1/1 (Skill) | — |
| 17 | `parseConfig` 어디 있어? | 비발동 | 0/1 (Grep) | 0/3 (Grep×3) | 0/1 (Grep) | 0/3 (Grep×3) |
| 18 | 검색식 한 번으로 `logger.info`의 정의 위치만 찾아줘 | 비발동 | 0/1 (Grep) | 0/3 (Grep×3) | 0/1 (Grep) | 0/3 (Grep×3) |
| 19 | `cache.py`의 캐시 설계 설명해줘 | 비발동 | 0/1 (Glob) | 0/3 (Glob×3) | 0/1 (Glob) | 0/3 (Read/Glob/Glob) |
| 20 | `a.py`, `b.py`, `c.py`, `d.py`, `e.py`를 읽고 각 파일의 역할을 한 줄씩 설명해줘 | 비발동 | 0/1 (Read) | 0/3 (Read×3) | 0/1 (Read) | 0/3 (Read×3) |
| 21 | `config.py` 내용을 읽고 설정 흐름만 설명해줘 | 비발동 | 0/1 (Read) | 0/3 (Read×3) | 0/1 (Read) | 0/3 (Read×3) |
| 22 | `pytest` 실행 후 종료 코드만 알려줘 | 비발동 | 0/1 (PowerShell) | 0/3 (PowerShell×3) | 0/1 (PowerShell) | 0/3 (PowerShell×3) |
| 23 | `npm run build`가 통과했는지만 확인해줘 | 비발동 | 0/1 (PowerShell) | 0/3 (PowerShell×3) | 0/1 (PowerShell) | 0/3 (PowerShell×3) |
| 24 | 안녕하세요 | 비발동 | 0/1 (없음) | — | 0/1 (없음) | — |
| 25 | 파이썬 딕셔너리와 리스트 차이를 짧게 설명해줘 | 비발동 | 0/1 (없음) | — | 0/1 (없음) | — |
| 26 | 네가 직접 이 에러 원인 찾아줘 | 비발동 | 0/1 (PowerShell) | 0/3 (Glob/Bash/PowerShell) | 0/1 (Glob) | 0/3 (PowerShell/PowerShell/없음) |

## 미검증·범위

- 이번 비교는 A6의 첫 도구 발동만 확인했다. 실제 작업 뒤 독립 검토·수행 중 전환 등 A8 실측은 이 요청 범위에 없어 새로 실행하지 않았다.
- 753자판의 채택 여부와 플러그인 버전 변경은 사용자 판단 대상이다. 두 `plugin.json`의 버전은 변경하지 않았다.
