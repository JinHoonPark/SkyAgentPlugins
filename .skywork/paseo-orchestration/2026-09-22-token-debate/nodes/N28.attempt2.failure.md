# N28 실패 — T12 description 재작업과 트리거 평가

## 결론

**A6·C3 실패.** `description` 987자는 A6 정적 조건을 만족하지만, 현재 재실행은 23개 중 14개 실패(`summary.failed = 14`)다. 고정 뒤 추가 수정하지 않았다.

## 수행 범위와 정적 검사

- 앞 시도 평가 파일 여섯 개를 `RUN/a6-attempt1-*.json|log`로, 실패 보고를 `RUN/nodes/N28.attempt1.failure.md`로 이름 변경해 보존했다. 현재 평가 세트 `RUN/a6-eval.json`은 `RUN/a6-attempt1-eval.json`과 바이트 단위로 같고 SHA-256은 `E6EF1CA9E669889A242EE552FF75E0680C288B430CEBA9D736EAC72A816477BE`다. 26개 질의, 발동 16개·비발동 10개, 비발동 근접 사례 8개를 유지했다.
- `git show 5e833be:plugins/paseo-toolkit/skills/agent-orchestration/SKILL.md` 종료 코드 0. 원문을 `RUN/a6-baseline-skill/SKILL.md`에 그대로 저장했다. 기준선 `description`은 323자다.
- `plugins/paseo-toolkit/skills/agent-orchestration/SKILL.md:3`의 frontmatter `description`만 직접 수정했다. 맨 앞에 첫 도구 호출 전 적용 판정과 적용 시 스킬 우선 읽기를 명시했다. **987자**, `<`·`>` 0개, YAML 파싱 성공, BOM 없음, CRLF 304개, CRLF→LF 기준 18,576자, 304줄이다. 본문은 바꾸지 않았다.
- `RUN/SPEC.md:18`의 R1-2 트리 12개 줄 조각을 공백 정규화 뒤 문구·번호·순서대로 대조했고 A6의 나머지 여덟 항목도 대조했다. 수정 뒤 정적 검사 종료 코드 0: `POST_STATIC_OK description=987 tree_segments=12 file_chars=18576 lines=304 eval_identical=26`.
- `python .skywork/paseo-orchestration/2026-09-22-token-debate/link-audit.py` 종료 코드 0, `source errors: 0`. 스킬 디렉터리에서 `python scripts/graph_update.py --self-test` 종료 코드 0, G1~G11 모두 통과했다.

## 평가 실행

- 세 평가 모두 `RUN/MILESTONE01-Tasks.md:138`의 PowerShell 블록을 사용했다. 승인된 절차에 따라 `finally`의 `Remove-Item -LiteralPath $work -Recurse -Force` 한 줄만 제외하고 `$work`를 실행 전에 출력했다. Windows의 cp949 기본 읽기를 피하려고 블록 밖 실행 환경에 `PYTHONUTF8=1`을 설정했다. 수정 평가 사본의 `--setting-sources project,local --strict-mcp-config`는 그대로이며 `--model`은 지정하지 않았다. 세 명령 블록 모두 종료 코드 0이다.
- 기준선: `--skill-path RUN/a6-baseline-skill`, `--eval-set RUN/a6-attempt1-eval.json`, `--runs-per-query 1`, `--trigger-threshold 0.5` → `RUN/a6-baseline-result.json`, `RUN/a6-baseline-stderr.log`. **summary.total=26, passed=11, failed=15**. 발동 기대 16개 중 1개, 비발동 기대 10개 중 10개가 맞았다. 원인 구분용 기준선이며 합격 판정에는 쓰지 않았다.
- 현재 1차: `--skill-path plugins/paseo-toolkit/skills/agent-orchestration`, `--eval-set RUN/a6-eval.json`, `--runs-per-query 1`, `--trigger-threshold 0.5` → `RUN/a6-result.json`, `RUN/a6-stderr.log`. **summary.total=26, passed=12, failed=14**. 발동 기대 2/16, 비발동 기대 10/10이다.
- 1차 불일치 14개와 미리 `boundary: true`인 13개의 합집합 **23개**만 `RUN/a6-rerun-eval.json`에 넣었다. 재실행하지 않은 3개는 1차 기대값과 일치한다. `--runs-per-query 3`으로 `RUN/a6-rerun-result.json`, `RUN/a6-rerun-stderr.log`에 저장했다. **summary.total=23, passed=9, failed=14**. 세 stderr 파일은 모두 0바이트로 질의 실패 경고가 없다.
- 앞 시도는 1차 12/26, 재실행 8/22였다. 현재는 1차 12/26, 재실행 9/23이다. 경계 사례의 1차 결과가 달라 재실행 대상 수가 22에서 23으로 바뀌었다. 현재와 기준선 결과 JSON·stderr에는 사용 모델 필드가 없어 **실제 모델은 확인 불가**다.

## 실패 질의 — 기대값과 실제값

아래 14개 모두 기대값은 발동이며, 현재 3회 재실행에서 요구된 2회 이상 발동에 미달했다. 표의 `n/1`, `n/3`은 첫 도구 호출에서의 발동 횟수다.

| 질의 | 기대 | 기준선 1회 | 앞 시도 1회 | 현재 1회 | 현재 3회 |
| --- | --- | ---: | ---: | ---: | ---: |
| 설정 오류 조사 작업은 워커에게 맡겨줘 | 발동 | 0/1 | 0/1 | 0/1 | 0/3 |
| 이 문서 정리 작업을 담당 워커에게 배정해줘 | 발동 | 0/1 | 0/1 | 0/1 | 0/3 |
| 이 에러 원인 찾아줘 | 발동 | 0/1 | 0/1 | 0/1 | 0/3 |
| 이 빌드 로그 분석해줘 | 발동 | 0/1 | 1/1 | 0/1 | 0/3 |
| docs/ 아래 30개 파일 링크 통일해줘 | 발동 | 0/1 | 0/1 | 0/1 | 0/3 |
| `a.py`, `b.py`, `c.py`, `d.py`, `e.py`, `f.py`의 헤더를 같은 형식으로 고쳐줘 | 발동 | 0/1 | 0/1 | 0/1 | 0/3 |
| `cache.py`에 만료 정책을 설계하고 구현해줘 | 발동 | 0/1 | 0/1 | 0/1 | 0/3 |
| `auth.py`의 새 정책 설계안을 작성하고, 구현한 다음 테스트 결과도 정리해줘 | 발동 | 0/1 | 0/1 | 0/1 | 1/3 |
| `a.py` 고치고 리뷰 에이전트한테 독립 검토 받아줘 | 발동 | 0/1 | 0/1 | 0/1 | 0/3 |
| `b.py` 변경안을 다른 에이전트에게 독립 검토 맡겨줘 | 발동 | 0/1 | 0/1 | 0/1 | 1/3 |
| 네가 직접 build 폴더 지워줘 | 발동 | 0/1 | 0/1 | 1/1 | 0/3 |
| 현재 브랜치에 git 커밋해줘 | 발동 | 0/1 | 1/1 | 0/1 | 0/3 |
| 네가 직접 브레인스토밍하자 | 발동 | 0/1 | 0/1 | 0/1 | 1/3 |
| 이 기능 아이디어 좀 내보자 | 발동 | 0/1 | 0/1 | 0/1 | 0/3 |

가능한 원인: 첫 도구 호출만 판정하는 하네스에서 스킬 선택 전에 다른 도구가 호출됐거나, `description`의 초기 명령문만으로 스킬 선택을 충분히 유도하지 못했을 수 있다. 기준선에도 “모든 작업 요청에 착수하기 전에 반드시 이 스킬을 읽고 적용한다”가 있었지만 발동 기대 1/16에 그쳤으므로, 명령문 부재만을 원인으로 확정할 수 없다. 결과 JSON은 발동 여부만 담아 실제 첫 도구와 정확한 원인을 가릴 수 없다. 이는 **추정**이며 재수정·재평가는 하지 않았다.

## 보존 파일과 남은 임시 폴더

- 앞 시도: `RUN/a6-attempt1-eval.json`, `a6-attempt1-result.json`, `a6-attempt1-stderr.log`, `a6-attempt1-rerun-eval.json`, `a6-attempt1-rerun-result.json`, `a6-attempt1-rerun-stderr.log`, `RUN/nodes/N28.attempt1.failure.md`.
- 기준선: `RUN/a6-baseline-skill/SKILL.md`, `RUN/a6-baseline-result.json`, `RUN/a6-baseline-stderr.log`.
- 현재: `RUN/a6-eval.json`, `a6-result.json`, `a6-stderr.log`, `a6-rerun-eval.json`, `a6-rerun-result.json`, `a6-rerun-stderr.log`.
- 기준선 임시 폴더: `C:\Users\magedoga\AppData\Local\Temp\skill-trigger-eval-5be6a3dd65f84dc2b73391cb3c73d62d`.
- 현재 1차 임시 폴더: `C:\Users\magedoga\AppData\Local\Temp\skill-trigger-eval-93274733c1b84de48b0d2a3e531319e8`.
- 현재 3회 재실행 임시 폴더: `C:\Users\magedoga\AppData\Local\Temp\skill-trigger-eval-c9158e27681e42888e5e20b59ec696d8`.
- 세 임시 폴더는 팀장이 정리한다. 앞 시도 임시 폴더는 이번 시작 시점에 남아 있지 않았다. 모든 현재 결과 파일이 RUN에 있고 JSON 파싱 및 재실행 대상 대조를 마쳤다.

## 미완료 범위

A6 발동 합격과 C3 트리거 평가 합격은 미완료다. `RUN/nodes/N28.md`는 합격 보고가 아니므로 만들지 않았다. 직접 수정 뒤 후발동과 직접·워커 지시 충돌은 이 평가 세트의 대상이 아니며 후속 실측에서 확인해야 한다. `RUN/SPEC.md`·`RUN/MILESTONE01-Tasks.md`와 두 `plugin.json`은 수정하지 않았고 Git 스테이징·커밋·푸시도 하지 않았다.
