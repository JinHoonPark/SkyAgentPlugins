# N8 — 스파이크 실행 결과

## 결론

- U1: **미판정** — 고정 모델 `claude-opus-5` 요청이 `401 authentication_failed`로 재시도되어 U1-4까지 완료하지 못했다.
- U2: **미실행** — 고정 모델 호출 실패 및 스냅숏 변경을 발견한 즉시 중단했다.
- U3: **미실행** — 고정 모델 호출 실패 및 스냅숏 변경을 발견한 즉시 중단했다.

## 실행·측정값

- 실제 `claude -p` 호출은 **1회**(상한 19회)이며, 요청·초기화 이벤트의 모델 ID는 `claude-opus-5`다 (`spike-logs/u1-init.jsonl:1`). 첫 API 재시도가 `401 authentication_failed`를 기록했다 (`spike-logs/u1-init.jsonl:2`). 대체 모델은 사용하지 않았다.
- U1 사전 조건은 Claude CLI·Python·인증 상태가 모두 확인됐다 (`spike-logs/u1-preflight.txt:1`, `spike-logs/u1-preflight.txt:3`, `spike-logs/u1-preflight.txt:14`). 두 `find_project_root()` 결과도 각각 skill-creator와 격리 디렉터리였다 (`spike-logs/u1-skill-creator-root.txt:1`, `spike-logs/u1-isolation-root.txt:1`).
- U1-3 초기화 이벤트에는 임시 stub 명령이 보이고 plugins는 빈 목록이었다 (`spike-logs/u1-init.jsonl:1`). 다만 모델 요청 실패로 smoke 2회는 실행하지 않았다.

## 스냅숏·격리

- 전후 설정 해시는 동일했지만, 네이티브 Git diff 해시가 `e6c635…`에서 `ac4641…`로 달라졌다 (`spike-logs/snapshot-comparison.txt:3`, `spike-logs/snapshot-comparison.txt:7`). `AGENTS.md`의 기존 수정 내용 변화가 관측됐으며, 이 작업에서 되돌리지 않았다.
- 허용 범위 밖의 새 파일도 발견됐다: WSL `~/.claude/plugins/` 계열 215개와 Windows `~/.claude/` 2개 (`spike-logs/snapshot-comparison.txt:16`, `spike-logs/snapshot-comparison.txt:17`). WSL `~/.claude/projects/`의 제외 대상 기록은 0개다 (`spike-logs/snapshot-comparison.txt:21`).
- `/tmp/paseo-orchestration-spike.*` 격리 fixture와 실행 프로세스는 종료·정리됐으며, 전역 설치본·설정의 후속 정리는 수행하지 않았다.

## 미검증 항목

- U1 smoke JSON 파싱·잔여 stub·하네스 판정은 미검증이다.
- U2의 8회 및 조건부 재실행, U3의 A/B 각 3회는 모두 미실행이다.
