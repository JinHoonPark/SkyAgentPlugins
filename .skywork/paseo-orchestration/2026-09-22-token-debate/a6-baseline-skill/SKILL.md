---
name: agent-orchestration
description: '팀장은 모든 작업 요청에 착수하기 전에 반드시 이 스킬을 읽고 적용한다. "조회해줘", "고쳐줘"처럼 할 일만 적힌 요청은 전부 이 스킬의 위임 대상이다. "브레인스토밍하자", "아이디어 좀 내보자", "뭘 만들면 좋을까", "방향 좀 잡아줘"처럼 무엇을 할지 아직 정해지지 않은 요청도 이 스킬이 받아 브레인스토밍 진입을 먼저 묻는다. 예외는 사용자가 팀장을 주어로 지목해 "네가 직접 해", "팀장이 직접 처리해"라고 지시한 경우뿐이다. 이 스킬을 통한 위임은 사용자의 상시 요청이므로 위임 여부를 다시 묻지 않는다. 라우팅은 팀장만 수행하며, 워커는 다시 위임하지 않는다.'
---

# Agent Orchestration

작업 지시를 받으면 리더인 너는 직접 착수하지 않는다. 작업을 워커에 맡기고, 결과를 취합해
사용자에게 보고하는 것이 이 스킬의 절차다. 작업 크기로 예외를 두지 않는다.

절차는 순서대로 1절부터 7절까지다. 레벨 2로 떨어지면 9절로 간다. 8절은 사용자가
상시 라우팅을 요청했을 때만 읽는다. 10절은 브레인스토밍 요청일 때 1절보다 먼저 읽는다.

## 리더가 직접 하는 일

워커가 원리적으로 못 하는 넷만 직접 한다.

1. 이 세션의 대화 맥락 자체를 근거로 삼아야 하는 일
2. 사용자와의 확인 문답
3. 워커 결과 취합과 사용자 보고
4. 사용자와의 브레인스토밍과 그 결과인 `BRAINSTORMING.md` 작성(10절)

파일과 설정은 대상이 이 세션 것이어도 워커에 맡긴다. 예외는 둘이다 — 사용자가
"이 세션에서 직접 하라"고 지시한 요청과, 내용이 이 세션 대화뿐인 `BRAINSTORMING.md`다.

`Agent` 도구·`Workflow`·deep-research는 쓰지 않는다.
단 스킬이 그 도구들을 쓰라고 지시하면 그 스킬을 따른다.

## 팀장 발화

팀장이 사용자에게 **먼저** 꺼내는 출력이 **자발 발화**다. 사용자 질문·지시에 답하는 출력은 자발 발화가 아니며, 아래 네 조건 중 **하나라도** 걸리는 사건에서만 자발 발화를 낸다. 넷 중 어디에도 걸리지 않으면 **침묵**한다. 어느 조건에 걸리는지 판정이 갈리면 즉시 발화로 본다.

1. **사용자 입력이 있어야 다음 동작이 정해진다** — 승인·확정·선택·답변을 받기 전에는 다음 동작을 고를 수 없다. 출력하고 턴을 마쳐 답을 기다린다.
2. **말하지 않고 진행하면 되돌릴 수 없는 결과가 남는다** — git 커밋·푸시·태그·브랜치 삭제, 외부 전송, 파일·디렉터리 삭제, 저장소 밖 설정 변경, 권한 상향.
3. **정해진 절차로는 더 진행할 수 없다** — 재시도·분기·폴백 중 어느 것으로도 재개 경로가 없다.
4. **기록과 실제 상태가 어긋나 지금의 기록을 믿고 판단하면 틀린다.**

침묵은 확인·검증·기록 의무를 줄이지 않는다. 침묵 구간에도 모델·모드·thinking 확인값 대조, 실패 보고 경로를 결과 파일보다 앞세우는 확인, 결과 파일과 합격 기준의 대조, `GRAPH.md` 갱신을 그대로 수행하고, 그 확인 결과는 완료 보고로 보낸다. **기동 시점 출력(7절)만 이 규칙의 예외다.** 사건별 즉시·침묵 배정은 [references/FORM.md](references/FORM.md)의 「발화 지점 배정」에 있고, 문서가 지시하는 출력은 전부 그 표에 배정돼 있다.

## 1. 전제 점검

### 도구를 부를 수 있는 상태로 만든다

claude에서 Paseo 도구는 deferred다. 스키마를 먼저 로드해야 호출할 수 있다.

```text
ToolSearch select:mcp__paseo__list_profiles,mcp__paseo__create_agent,mcp__paseo__list_workspaces,mcp__paseo__send_agent_prompt,mcp__paseo__get_agent_status,mcp__paseo__list_agents,mcp__paseo__cancel_agent
```

필요한 도구가 늘면 같은 형식으로 이어 붙인다. **이 호스트에 `ToolSearch`가 있으면** 그
줄로 스키마를 먼저 로드한다. 없으면 이미 노출된 도구 목록과 현재 검색 수단으로 같은
이름을 확인한다. Codex라고 해서 `ToolSearch`가 없다고 단정하거나 특정 모드를 강제하지
않는다. 이후 절차의 도구 이름(`list_profiles`, `create_agent`, `list_workspaces`,
`send_agent_prompt`, `get_agent_status`, `list_agents`, `cancel_agent`)은 접두어 없이 쓴다.

Paseo 도구가 이 세션에 붙어 있지 않으면 연결이 불충족이다. 그 사실을 명확히 보고하고
사용자 전역 config를 스스로 고치지 않는다. 프로필 조회는 `list_profiles` 하나뿐이고
대응하는 CLI 명령이 없다. provider를 추측하지 않는다. (`daemon.mcp.injectIntoAgents`가
활성이어야 새 세션에 Paseo 도구가 주입된다. 없으면 기본은 false다. 연결이 실패하면 이미
승인된 범위 안에서 복구하고, 복구할 수 없으면 필요한 설정 변경을 구체적으로 제시해 사용자
판단을 받는다.) 사용자 판단을 받은 뒤에만 `paseo run`의 `--provider`·`--mode`·`--thinking`으로
기동한다. 기동 후 응답·`inspect`의 `thinking`/`currentModeId` 값을 요청값과 대조해 적용
여부를 확인하고, 확인됐는지 못 했는지를 기동 보고(7절)에 적는다.

### 프로필 목록을 읽는다

`list_profiles`로 모든 프로필의 `notes`를 읽고 `notes`가 맞는 프로필을 고른다. 추측하지 않는다. 같은 세션에서 이미 읽었으면 다시 호출하지 않는다.
**예외 — 사용자가 그 세션에서 프로필을 바꿨거나 갱신이 확인되면 `list_profiles`를 다시
호출한다.** 매 기동마다 재조회하지는 않는다.

| 상태 | 판별 | 처리 |
| --- | --- | --- |
| **A. 목록이 비었다** | `list_profiles`가 빈 목록 | provider를 추측하지 않는다. 프로필이 없다고 알리고 진행 여부를 묻는다. 진행하겠다고 답하면 `list_providers`·`list_models`로 고른 뒤 그 사실을 완료 보고에 남긴다 |
| **B. 맞는 것이 없다** | 어느 `notes`도 이 작업 유형을 포함하지 않는다 | 어느 프로필도 억지로 고르지 않는다. provider를 추측하지 않는다. 어떤 유형을 찾았고 어느 `notes`도 걸리지 않았는지 한 줄로 알리지 않고, 4절의 역할↔프로필 해석 4단계로 이번 실행용 값을 구성해 그 사실을 완료 보고에 남긴다 |
| **C. 부분적으로만 맞는다** | `notes`가 인접하지만 경계 문구가 이 작업을 배제한다 | 허용되는 부분만 분리해 배정하고, 나머지는 적합 프로필 없음으로 처리한다(B). 통지로 경계를 넓히지 않는다. provider를 추측하지 않는다. 벗어나는 부분이 작업의 본체이면 B로 취급한다. 분리 배정한 사실은 완료 보고에 남긴다 |

### 워크스페이스를 고른다

`list_workspaces`로 조회해 `cwd`가 대상 저장소인 워크스페이스를 고르고, 그 `workspaceId`를
`create_agent`에 **명시한다.** 생략하면 새 워크스페이스가 만들어진다. 워크스페이스 ID를
하드코딩하지 않고 매번 조회한다.

결과 파일을 서로 읽어야 하는 작업들은 같은 워크스페이스에 둔다. 같은 파일을 여럿이 써야 하는 요청은 2절 Step 3에서 레벨 2로
떨어지므로 격리로 우회하지 않는다. worktree 격리는 사용자가
명시할 때만 쓰고, 그때는 각 worktree 경로를 받아 그 경로를 읽어야 하는 워커의 브리핑에 넣는다.

이 호스트에서 쓸 수 있는 완료 알림·대기 수단을 확인하고, 없으면 종료 확인에
`get_agent_status`·`list_agents`의 제한된 상태 조회를 허용한다.

## 2. 레벨 판정 게이트

대상·역할·공통 입력을 기준으로 작업을 서로 겹치지 않는 단위로 먼저 쪼갠다. 단위 하나 =
워커 하나. 맥락이 다른 일은 나눈다 — 한 세션에 여러 맥락을 몰지 않는다. 워커를 하나라도
띄우기 전에, 그 단위들 사이의 의존을 이 절의 트리로 판정한다.

### 개발 워크플로우 단계

**레벨을 개발 성숙도로 해석하지 않는다.** 단순 조회와
명백한 소규모 수정에는 스펙·마일스톤을 강제하지 않는다.

작업이 지나는 단계는 다음 순서다.

1. 스펙 작성 ↔ 사용자 피드백
2. 사용자 스펙 확정
3. 스파이크 필요성 **공동 판정**
4. 필요할 때 스파이크 설계·사용자 확정·실험 실행·사용자 확인
5. 마일스톤 필요성 **공동 판정**
6. 필요할 때 마일스톤 작성·사용자 확정
7. 태스크 분해·사용자 확정
8. 구현·자체 테스트
9. 동작하는 기능 또는 마일스톤 단위 독립 리뷰 1회

2, 4, 6, 7은 사용자 확정 게이트고, 4와 6은 3과 5의 공동 판정이 필요일 때만 돈다. 생략이면 그 단계를 건너뛰고 다음 단계로 간다.

독립 검토 노드를 여는 경우는 **셋뿐이다** — ① 동작하는 기능 또는 마일스톤 구현이 끝난 뒤 여는 1회(9번), ② 여러 후속 작업을 폐기할 수 있는데 실험이나 기존 코드로 확인되지 않은 결정에 대해 팀장이 대상을 지정한 중간 검토, ③ 사용자가 명시적으로 요청한 검토. 스펙·스파이크·마일스톤·태스크 분해 단계에는 검토를 자동으로 붙이지 않는다. 워커의 자체 검증과 팀장의 결과·실패 증빙 확인은 독립 검토가 아니다.

스파이크·마일스톤의 필요 여부는 담당 전문 역할 워커가 판정안을 내고, 팀장이 세션 맥락·사용자
확정 사항과 대조해 공동 결론을 낸다. **`팀장 단독` 판정은 금지한다.** 필요 없다는 판정도 정상
산출물이며 실패가 아니다.

스펙 작성 워커가 결정이 필요한 질문을 만들고 팀장이 의미를 바꾸지 않고 사용자에게 중계하며, 사용자 답변은 그 질문과 묶어 원문으로 같은 작성 워커에게 전달한다.

여러 작업에 같은 내용이 필요하면, 그것이 이미 확정된 공통 입력인지 먼저 확인한다.
확정됐으면 리더가 같은 내용을 각 워커에 넘겨 독립 노드로 둔다. 확정되지 않았으면 작성
노드 → 사용 노드의 다단이다. 리더가 독립성을 만들려고 그 내용을 직접 창작하면 「리더가
직접 하는 일」의 제한과 충돌한다. 그 경우는 다단으로 간다.

대상 파일이 다르다는 사실만으로 무조건 분리하지는 않되, 공통 입력이 있다는 이유로 한
워커에 합치지도 않는다.

### 같은 목적의 작업 묶음

동일한 목적·역할·확정 입력을 공유하는 정확 검색과 미리 지정된 명령은 **횟수와 무관하게 한 작업 단위로 묶는다.** 같은 조회를 여러 단위로 나눠 여러 워커에 맡기지 않는다.

묶기 전에 각 항목에 Step 1~5의 의존·역할 분리·공동 쓰기·판정 의존·불명확성이 없는지 확인하고, **하나라도 있으면 묶지 않는다.** 출력에 따라 다음 명령을 고르는 작업은 이 묶음에서 제외하고 `## 4. 프로필 선택과 값 이관`의 역할 해석으로 보낸다.

묶음 전체에도 Step 0과 6절의 실패·권한 규칙을 적용한다. 묶음으로 Step 0 승인, Step 1~5 레벨 판정, 작성자와 검토자 분리를 우회하지 않는다.

**순서가 있는 판정 트리다. 위에서부터 내려가 처음 걸리는 곳에서 멈춘다.** **워커 수는 어느
단계에도 나오지 않는다.**

```text
Step 0 (레벨과 직교, 항상 수행)
  되돌리기 점검 → 걸리면 그 작업에 별도 게이트를 단다. 레벨은 바꾸지 않는다.

Step 1  어떤 작업의 입력이 다른 작업의 출력인가?            예 → 레벨 2 (멈춤)
Step 2  같은 대상에 서로 다른 역할이 배정되는가?            예 → 레벨 2 (멈춤)
          (구현과 검토, 토론자와 사회자, 작성과 감수)
Step 3  둘 이상의 작업이 같은 파일을 쓰는가?                예 → 레벨 2 (멈춤)
Step 4  어떤 작업의 합격 기준 판정이 다른 작업의 결과에      예 → 레벨 2 (멈춤)
        걸려 있는가?
Step 5  Step 1~4 중 하나라도 "모르겠다"인가?                예 → 레벨 2 (멈춤)
Step 6  그 밖의 전부                                       → 레벨 1
```

쪼갠 결과 다른 작업이 없어도 트리를 건너뛰지 않는다. Step 6에서 레벨 1이 된다.

### 레벨 2로 떨어지면 9절로 간다

Step 1~5 중 하나에 걸리면 **레벨 1로 처리하지 않는다.** 프로필 선택·기동 어느 것도
3절로 내려가지 않는다. 어느 Step에 무엇 때문에 걸렸는지는 따로 알리지 않고 승인 화면의
mermaid 그래프가 드러낸다. 9절로 간다.
순서를 바꿔 순차로 돌리는 것으로 레벨 2가 레벨 1이 되지는 않는다.

### Step 0 — 되돌리기 어려운 작업

레벨과 직교하므로 **레벨 1 경로에서도, 워커가 하나뿐인 요청에서도 건너뛰지 않는다.**

범위는 넷이다 — git 커밋·푸시·태그·브랜치 삭제, 외부 전송, 파일·디렉터리 삭제, 저장소 밖
설정 변경.

- 걸리는 단위가 있으면 그 단위의 기동 **직전에** 승인을 받는다. 걸리지 않는 나머지 단위는
  승인을 기다리지 않고 기동한다.
- 사용자가 그 되돌리기 어려운 작업을 이미 명시적으로 지시했으면 그 지시가 곧 승인이다. 리더가 스스로
  추가한 되돌리기 어려운 행동은 언제나 승인 대상이다.
- git 커밋·스테이징·푸시·태그·브랜치 삭제는 사용자 명시 승인 없이 워커에 맡기지 않는다.
- 무응답·답변 누락·주제 전환·다른 작업 지시는 승인이 아니다.

## 3. 레벨 1 — 팬아웃

2절에서 레벨 1로 판정된 요청만 이 절로 온다.

1. 단위마다 프로필을 고른다(4절). 단위 성격이 같으면 같은 프로필을 써도 된다.
2. 되돌리기 어려운 단위를 점검한다(2절 Step 0). 걸린 노드는 승인 화면이 아니라 그 노드
   기동 **직전의** 별도 승인으로 처리한다.
3. 활성 큐를 채워 기동한다. 승인 문답은 하지 않는다(되돌리기 어려운 단위 제외). 레벨 1에는
   계획 승인 화면이 없다.
4. 활성 큐의 `create_agent`를 호출하기 **전에** 실행 디렉터리의 `GRAPH.md`에 mermaid 블록과
   노드 행을 한 번 쓴다 — 경로·형식은 `references/level1.md`의 「레벨 1의 `GRAPH.md` 기록」.
   각 노드의 `agentId`는 받는 즉시 그 노드 행에 채운다.
5. 완료 대기는 1절에서 확인한 수단을 쓴다. 알림이 있으면 폴링하지 않는다. 종료마다 아래
   투입 절차를 돈다.
6. 전원 완료 후 취합해 7절 형식으로 보고한다.

작업 개수가 많다는 것 자체는 승인 사유가 아니다.

레벨 1의 실행 기록 생성·갱신과 첫 기동 시의 목적 머리말·`GRAPH.md` 경로 출력은
[references/level1.md](references/level1.md)와 7절에 있다. 레벨 1로 판정했으면 기동 전에 읽는다.

### 활성 큐와 대기 큐

| 개념 | 값·정의 |
| --- | --- |
| **활성 큐** | 지금 실행 중인 워커. 동시 실행 상한 10 |
| **대기 큐** | 아직 기동하지 않은 작업. 상한 없음. 순서는 네가 쪼갠 순서(FIFO) |

작업마다 이만큼을 들고 있는다 — 노드 ID(워커는 `N`, 제어는 `G`, 접두별 독립 번호이고 한 번 준 ID는 바꾸지 않는다 — 9절 「설계」) / 상태(`대기`·`실행 중`·`완료`·`실패`·`생략`) /
프로필 / 대상 / 결과 파일 경로 / agentId(기동 후) / 되돌리기(예·아니오) / 재시도 가능 여부(6절).

생략 판정을 받은 단계는 워커를 만들지 않는다. 상태를 `생략`으로 두고 `create_agent` 대상에서
제외한다.

**큐 상태를 별도 파일로 만들지 않는다.** 큐는 네 컨텍스트에만 둔다. 이 금지는 실행
기록(`GRAPH.md`)에는 미치지 않는다 — 노드별 `상태`·`agentId`는 `GRAPH.md` 하나에 남긴다(`references/level1.md`·`references/level2.md`).

투입 절차 — 1절에서 확인한 수단으로 종료를 기다린다. 종료 1건을 확인하면:

1. 종료를 확인한 뒤 실패 보고와 합격 기준을 대조해 판정한다 — 파일이 없다는 사실만으로
   성공을 판정하지 않는다(`references/level2.md`의 「실행」 3). 활성 슬롯 1개가 빈다.
2. 판정을 그 노드의 행에 반영한다 — `GRAPH.md` 표에서 그 행의 `상태` 열만 바꾼다. 표를
   통째로 새로 쓰지 않고 mermaid도 건드리지 않는다(`references/level1.md`의 「레벨 1의 `GRAPH.md` 기록」).
3. 대기 큐가 비어 있지 않으면 선두 1개를 기동하고 `실행 중`으로 바꾼다.
4. 알림이 여러 건 몰려 오면 **해제된 슬롯 수만큼** 투입한다. 상한을 넘겨 기동하지 않는다.
5. 대기 큐가 비고 활성 큐도 비면 취합·보고로 넘어간다 — 이때 `GRAPH.md`의 상태를 최종
   상태로 확정한다.
6. 기동이 실패하면 그 작업은 활성 슬롯을 잡지 않은 것으로 보고 6절의 재시도 분류를 따른다.

레벨 1 재개와, 대기 중 작업이 실패한 앞 작업의 결과 파일을 필요로 한다는 것을 뒤늦게
발견했을 때의 처리는 같은 [references/level1.md](references/level1.md)에 있다.

## 4. 프로필 선택과 값 이관

### 역할↔프로필 해석

워크플로우 단계와 그래프 패턴은 **역할 이름**으로 기술한다 — 스펙 작성, 스파이크 설계,
구현, 독립 검토처럼 일의 종류다. **프로필 id는 실제 선택 결과와 프리셋 예시에만 쓴다.**

역할에 쓸 프로필은 아래 순서로 해석한다. 앞 단계에서 확정되지 않을 때만 다음 단계로 간다.

1. **`notes`가 그 역할의 핵심 작업을 명시적으로 허용하는 프로필을 쓴다.** 스펙 작성 역할의
   핵심 작업에는 요구사항 분석과 사용자에게 확인할 질문 생성이 포함된다. 그 작업을 허용하는
   `notes`인지로 판단하며, 이름이나 id가 `spec`이라는 이유만으로 허용된 것으로 보지 않는다.
2. **이름·id가 달라도 같은 범위를 허용하는 넓은 프로필이면 쓴다.** 역할 이름과 프로필 id를
   일대일로 강제하지 않는다. `notes`가 그 작업 범위를 포함하면 그 프로필을 쓴다.
3. **담당 부분만 안전하게 분리할 수 있으면 분리 배정한다.** 분리된 나머지는 다음 단계로 간다.
   안전하게 나눌 수 없으면 4단계로 간다.
4. **없으면 공용 프리셋 `../../references/presets.md`의 역할 정의와 실조회 모델·thinking으로
   이번 실행용 값을 구성한다.** 프리셋의 「역할 정의」·「역할별 thinking 등급」·「등급 해석」
   절을 **이때 읽는다.** 전역
   `agentProfiles`를 자동으로 바꾸지 않는다. 구성한 값의 출처를 보고에 남긴다.

`notes`의 경계가 배제한 일은 「인접 역할」이라는 이유로 넘기지 않는다. 어느 단계에서도
적합한 `notes`를 찾지 못했으면 그 사실을 숨기지 않고 보고한다.

### 값 이관

`create_agent`에는 `profile` 인수가 없다. **프로필의 값은 자동으로 적용되지 않으므로 네가
옮겨 담는다.** 이름이 서로 다르니 아래 대응을 그대로 따른다.

| 프로필의 값 | `create_agent`의 자리 |
| --- | --- |
| `provider` + `model` | `provider` — 두 값을 `provider/model` 한 문자열로 합쳐 넣는다 |
| `modeId` | `settings.modeId` |
| `thinkingOptionId` | `settings.thinkingOptionId` |
| `featureValues` | `settings.features` — **프로필에 이 키가 있을 때만** 옮긴다. 없으면 `settings.features`를 넣지 않는다 |

- `workspaceId`를 명시한다(1절).
- `title`은 `프로필이름: 한 줄 요약`, `labels`는
  `{"profile": 프로필id, "task": 한 마디, "run": 실행 식별자, "node": 노드 ID}`.
  `run`은 이번 실행의 실행 디렉터리 이름 `{YYYY-MM-DD}-{슬러그}`다. `title`은 60자 상한이므로
  그 안에서 만든다. 프로필 이름이 길어 요약 여유가 10자 미만이면 `title`에 요약만 쓰고
  프로필은 `labels.profile`로만 남긴다.
- `settings.modeId`는 스키마상 선택 필드지만 **채우는 것을 기본으로 둔다.** 프로필의 값을
  그대로 옮긴다.
- 실제 모드·모델·thinking은 요청값이 아니라 기동 결과로 확인한다. 모드는 `create_agent`
  응답의 `currentModeId`를 요청값과 대조한다. 모델과 thinking은 `snapshot.model`·
  `snapshot.effectiveThinkingOptionId`로 확인한다. `runtimeInfo`는 쓰지 않는다. 노드 1~2개면 `create_agent` 직후
  `get_agent_status` 1회, 팬아웃이면 전체 기동 후 `list_agents` 1회로 한꺼번에 대조한다.
  호출 수가 노드 수에 비례하지 않게 한다. 보고의 "모드"·"모델" 줄에는 확인한 값을 쓰고,
  요청값과 다르면 출처를 적는다(7절).
- 결과 파일을 써야 하는 단위에는 claude의 `plan` 모드를 고르지 않는다.

### 자문 역할의 폴백

자문 역할에는 **다른 역할로의 대체를 두지 않는다.**

1. 적합 프로필이 없으면 공용 프리셋 `../../references/presets.md`의 자문 역할 정의와 고성능
   등급 해석 절차로 이번 실행용 값을 구성한다. 자문의 **사용자 승인 조건은 그대로** 적용한다
   — 사용 목적의 승인이 없으면 띄우지 않는다.
2. 그것도 불가능하면 자문 노드를 생략하고 사용자에게 보고한다.

**자문을 다른 역할의 폴백으로 쓰지 않는다.** 자문 역할이 탐색·검토 등 다른 역할의 폴백이
되지 않고, 다른 역할의 부족을 자문 프로필로 메우지도 않는다.

## 5. 워커 브리핑

브리핑은 이 작업의 수행·권한·판정에 필요한 정보만 한 번씩 싣고, 같은 내용을 반복하지 않는다.

`assets/worker-briefing.md`가 그 템플릿이다. **워커를 띄우기 직전에 읽고** 중괄호 자리를 채워 `initialPrompt`로 넘긴다.

반드시 싣는 것:
1. 워커 신분과 세션 내 재위임 금지
2. 실제 선택 프로필 이름과 그 프로필의 `notes` 원문 — 첫 줄 고정 형식은 `너는 워커다. 프로필: {이름} — {notes 원문}`이다.
3. 노드 목표, 수정 가능 대상, 사용자 확정 제약
4. 담당 원본 합격 기준 — 네가 요약한 것이 아니라 원본을 옮긴다
5. 필수 입력 경로
6. 결과 보고 경로와 실패 보고 경로(둘 다 네가 지정한다)
7. 도구와 권한 예외 — 셸은 기본 도구로 실행하고 터미널을 새로 만들지 않는다
8. 출력 크기 제한
9. 보고 형식

싣지 않는 것:
1. 프로필 선택 이유와 모델 비교
2. 큐 운영과 계획 승인 절차
3. 다른 노드의 작업과 참고 배경
4. 같은 제약의 반복
5. 미승인 제안
6. 경로로 제공한 앞 산출물의 본문
7. 검토 노드에 주는 작성자 보고와 해결 방향

사용자 대화에만 있는 제약은 브리핑에 싣는다. 스펙 질문과 그 사용자 답변은 제외 목록의 좁은 예외로서 조건과 예외까지 원문으로 전달한다. 미확정 요청과 질문 대안은 미확정 입력으로 표시하고 실행 지시나 확정 제약으로 취급하지 않는다.

전체 대화와 산출물 본문은 다시 보내지 않는다. 글자 수 때문에 포함 목록 항목을 빼지 않는다. 추가 맥락은 빠지면 실행이나 판정이 달라지는 경우에만 싣는다. 프로필 `notes`는 요약 없이 원문을 한 번만 싣는다. 새 브리핑 문서는 만들지 않는다.

### 검토는 만든 워커에게 맡기지 않는다

결과 파일이 합격 기준을 충족하는지 판정하는 일을 그것을 만든 에이전트에게 맡기지 않는다. 검토 워커는 아래 세 조건을 **모두**
만족해야 한다.

이 절은 **9번 기본 독립 리뷰, 팀장이 지정한 중간 검토, 사용자가 요청한 검토**에만 적용한다 — 모든 개발 단계에 검토를 자동으로 붙이라는 뜻이 아니다.

1. **작성 워커와 다른 `agentId`다.** 같은 에이전트에 재프롬프트해 검토를 시키지 않는다.
2. **작성자의 대화·보고를 입력으로 받지 않는다.** 넘기는 것은 **원본 합격 기준과 대상
   경로**다.
3. **선택한 프로필의 `notes`가 검토를 허용한다.** `notes`가 검토를 금지하는 프로필은 검토에
   쓰지 않는다.

검토 라운드는 같은 단계·같은 수용 기준에 대해 총 3라운드이고 최초 검토 1회와 재검토 최대 2회다.
검토를 시작하기 전에 상한과 사용 횟수를 `GRAPH.md`의 해당 노드 행에 기록한다. 상한을 소진하면 결론을 지어내지 않고 남은 지적과 각 라운드 검토 결과 파일 경로를 보고한 뒤 멈춘다. 추가 라운드는 횟수와 대상을 정한 사용자 결정으로만 연다.

**전용 검토 프로필이 있으면 우선한다.** 없으면 4절의 역할↔프로필 해석으로 검토 프로필을
고른다. 구현과 검토를 함께 요청받은 것은 2절 Step 2에 걸린다. 레벨 2이므로 9절로 간다. 검토
노드를 띄우기 직전에 [assets/review-briefing.md](assets/review-briefing.md)를 읽고
중괄호를 채워 `initialPrompt`로 넘긴다.

### 결과 파일로 받는다

결과는 완료 알림 본문이 아니라 파일로 받는다.

완료 알림에는 결론, 수행 범위, 해당 핵심 실행 증빙, 핵심 근거, 제약, 해당 결과 경로를 짧게 적고 해당하지 않는 항목은 생략한다. **출력 제한 때문에 사용자 요구 데이터나 합격 판정 근거를 빼지 않는다.** 긴 텍스트 산출은 별도 결과 파일에 필요한 부분과 위치를 보존하고, 파일 수정 자체가 결과인 작업은 별도 성공 보고 파일을 만들지 않는다.

- 조사·분석처럼 결과가 텍스트라면 실행 디렉터리를 정한다:
  `.skywork/paseo-orchestration/{날짜}-{슬러그}/`. 날짜는 실행일의 `YYYY-MM-DD`이고
  슬러그는 이번 실행 전용 값이다. 같은 경로가 이미 있으면 슬러그를 바꿔 새 디렉터리를
  고른다. **단계 산출물은 예외다** — 스펙·스파이크 설계·마일스톤·태스크 분해 산출물은 실행
  디렉터리 바로 아래 `SPEC.md`, `SPIKE.md`, `MILESTONES.md`, `MILESTONE{NN}-Tasks.md`에
  쓰고, 이 넷에는 일반 노드 결과 경로를 쓰지 않는다. `{NN}`은 마일스톤 번호이지 노드 번호가
  아니다. 그 밖의 조사·분석·판정·검토 결과는 작업마다 `nodes/{노드 ID}.md`, 실패 보고는
  `nodes/{노드 ID}.failure.md`에 쓰게 한다 — 반드시 서로 다른 파일이다. **검토를
  2라운드 이상 도는 노드는 예외다** — 그 노드의 2라운드 이후 결과는
  `nodes/{노드 ID}.round{N}.md`, 실패 보고는 `nodes/{노드 ID}.round{N}.failure.md`에 쓰고
  1라운드 파일을 덮어쓰지 않는다(`references/level2.md`). 이 실행
  디렉터리와 두 파일명은 **레벨 1·2 공용**이고, 그 디렉터리의 `GRAPH.md`가 실행
  기록이다(`references/level1.md`·`references/level2.md`). 경로는 네가 정해 브리핑에 명시한다. 스킬 소스에 사용자의 고정
  경로를 하드코딩하지 않는다 — 실행 중에는 선택한 workspace 기준 상대 경로로 해석한다.
  worktree 간 전달이나 호스트가 절대 경로를 요구할 때는 조회로 확인한 실제 절대 경로를
  그대로 전달한다.
- 결과가 파일 수정 그 자체라면 성공했을 때 쓸 별도 결과 파일을 만들지 않는다. 그래도
  **실패 보고 경로는 지정한다** — 같은 실행 디렉터리의 `nodes/{노드 ID}.failure.md`다. 성공하면 그
  파일이 생기지 않고, 파일이 있으면 그것이 실패다.
- **수정 대상 파일에 실패 사유를 쓰게 하지 않는다.** 실패 사유는 언제나 실패 보고 경로로 간다.
- 파일 기록이 불가능하거나 사용자가 쓰기를 금지한 경우, 워커는 파일을 쓰지 않고 짧은
  응답으로 실패와 미검증을 보고한다. 그 응답을 실패 보고로 본다.
- **결과 파일 본문을 네 컨텍스트로 옮기지 않는다.** 판단이 필요할 때만 알림 요약을 읽고, 부족하면 결과 파일을 범위를 제한해 읽는다.

## 6. 실행 중 개입과 실패 처리

실패 분류·재시도 한도·셸 실패·프로바이더 장애·재개의 동작은 그때
[references/failure-handling.md](references/failure-handling.md)를 읽는다.

### 주입 — 승인이 필요한 것과 아닌 것

실행 중인 워커에 지시를 보내는 수단은 `send_agent_prompt`다. 그 워커를 폐기할 때만
`cancel_agent`를 쓴다. **이 호스트가 실행 중 메시지·모드 변경을 막으면 그 정책을 이 표보다
앞세운다.** 막히면 우회하지 않고 보고한다.

| 종류 | 예 | 승인 |
| --- | --- | --- |
| **조회형** | 진행 상황 질의, 지금까지 쓴 결과 파일 경로 확인 | 불필요 |
| **범위 내 명확화** | 결과 파일 경로 재지시, 출력 형식 지정, 이미 쓴 결과 파일의 처리 지시(덮어쓰기/이어 쓰기/버리기) | 불필요 |
| **범위 변경** | 합격 기준의 변경·추가, 대상 파일 추가, 역할 변경, 되돌리기 어려운 행동 추가, 권한을 올리는 모드 변경 | **필수 — 보내기 전에** |

승인 없이 보낸 뒤 사후에 알리면 게이트를 우회한 것이다. 애매하면 범위 변경으로 본다.
이미 받은 계획·되돌리기·권한 승인은 같은 범위에서 다시 묻지 않고 재사용한다. 레벨 2에서
범위 변경을 승인받으면 보낸 뒤 `GRAPH.md`의 해당 노드 행을 갱신한다(`references/level2.md`의 「기록 시점」).

### 실패

| 상황 | 처리 |
| --- | --- |
| 워커 실패 | 참조의 재시도 분류를 따른다. 무조건 자동 재시도는 하지 않는다 |
| 브리핑 결함으로 실패 | 재시도하지 않고 원인과 함께 보고한다 |
| 권한 대기 | 기존 승인 범위 안이면 재사용해 응답한다. 없거나 범위를 넘으면 요청 원문을 그대로 보여주고 사용자 판단을 받는다 — 임의로 자동 승인하지 않는다 |
| 명시 거부 | 재시도하지 않는다. 모드를 올려 우회하지 않는다 |
| 사용자 중단 | 실행 중 전원을 중단하고 작업별 상태와 남은 결과 파일 경로를 보고한다 |
| 부분 실패 | 작업별 상태(완료/실패/미기동) + 완료된 결과 파일 경로 + 무엇을 못 했는지 |

자동 재시도는 참조의 두 조건(부수효과 없음 + 멱등)을 모두 만족할 때만, 1회만 한다. 호출
**전에** 사용 횟수를 기록한다. 생성 결과가 불명이면 재기동하지 않고 조회부터 한다. 실패
보고 경로에 내용이 있으면 그것을 완료보다 앞세운다. 레벨 2는 재시도 셀에 한도와 사용
횟수를 적는다(`references/level2.md`).

### codex 셸 · 프로바이더 장애

셸 실패를 `full-access`로 자동 승격하지 않는다. 호스트 정책이 허용하고, 그 권한이 이미
승인됐거나 새로 승인된 뒤에만 모드를 바꾼다. 프로바이더 장애는 자동 대체하지 않는다.
동작은 참조를 따른다.

## 7. 취합과 보고

워커를 띄웠으면 반드시 안내한다. 작업 크기로 생략하지 않는다. 워커 하나당 코드블록
하나다. 여러 개를 띄웠으면 블록을 연달아 놓는다. 한 블록에 여러 건을 넣지 않고, 블록
사이에 구분자를 넣지 않는다. 표로 바꾸지 않는다.

출력하기 직전에 [references/FORM.md](references/FORM.md)의 `## 기동 보고`를 읽는다. 라벨
구성(`이름`·`모델`·`모드`·`작업`)과 모델·모드 확인값(`snapshot.model`·
`snapshot.effectiveThinkingOptionId`·`currentModeId`) 표기가 그 절에 있다.

**레벨 1**은 그 실행에서 처음 워커를 기동할 때만 코드블록들 **앞에** 목적 머리말 한 줄과
`GRAPH.md` 경로 한 줄을 낸다. 두 번째 이후 기동에는 그때 기동한 워커의 코드블록만 낸다.
**레벨 2**는 승인 화면에서 목적 머리말과 `GRAPH.md` 경로를 이미 냈으므로 코드블록만 낸다.
**기동 시점에 mermaid 블록을 내지 않는다.** `GRAPH.md` 경로는 실행당 한 번이고 워커 코드블록
안에 넣지 않는다. 이 출력은 승인 요구가 아니다 — 승인 문답을 새로 만들지 않는다.

- 요청값과 다르면 출처를 적는다.
- `paseo run` CLI로 기동했으면 `thinkingOptionId`가 유실될 수 있다는 사실을 함께 적는다(1절).
- **쪼개기와 프로필 선택의 과정은 출력하지 않는다.** 결과만 낸다.

전원 완료 후 사용자에게 결과를 정리해 보고하는 최종 취합은 리더가 한다. 워커에 맡기지
않는다. 여러 워커의 결과 파일을 입력으로 새로운 비교·분석 결과 파일을 만드는 작업은 워커에
맡길 수 있다. 완료 보고에 반드시 들어갈 항목은 FORM.md의 `## 완료 보고`에 있다.

### 결과 인수

팀장은 종료 요약의 수행 범위·핵심 근거·해당 종료 코드·누락·미검증을 **원본 합격 기준과 대조하고**, 근거 없이 완료로 인정하지 않는다. 원문 전체 재독이나 동일 검색·명령의 전면 재수행을 기본으로 하지 않는다. 근거 불일치·필수 데이터 누락·중요 결함 의심이 있으면 확인 범위를 특정해 그 부분만 본다. **이미 낸 증빙의 필요한 부분을 확인하는 것은 결과 인수다.**

후속 작업은 넷을 구분한다.
1. **팀장의 결과 인수** — 위 대조까지. 새 워커를 띄우지 않는다.
2. **원래 역할 워커의 범위 내 후속 보완** — 같은 역할·같은 합격 기준 안의 수정이다.
3. **역할이 달라지는 후속 작업** — 4절의 역할 해석에 따라 적합 프로필로 보낸다.
4. **별도 워커의 독립 검토** — 원본 요구나 개발 워크플로우·승인 그래프가 요구할 때만 둔다.

**결과 인수는 독립 검토를 대신하지 않는다.** 독립 검토의 기존 세 조건은 그대로 유지한다.

## 8. 세션이 이 스킬을 참조하지 않고 지나갈 때

사용자가 상시 프롬프트 추가를 요청한 경우에만 `scripts/append_system_prompt.py`를 실행한다.
요청이 없으면 이 절을 실행하지 않는다. **이 절차를 실행할 때 스크립트를 읽는다.**

대상 필드는 `daemon.appendSystemPrompt` 하나다. 파일 경로는 스크립트가 정한다. 다른 후보를
두지 않는다. 값은 덧붙이기만 하며 제거 경로는 없다. 기본이 dry-run이라 승인 전에 제시할 수
있다.

1. **점검·제시** — `--value`에 추가할 한 줄만 주고 실행한다. 그 줄이 이미 있으면
   `alreadyPresent`가 참으로 오고 아무것도 하지 않는다. 그 사실을 알리고 끝낸다.
   ```text
   python scripts/append_system_prompt.py --value "추가할 한 줄"
   ```
2. **승인** — dry-run이 낸 넷을 그대로 보여주고 명시 승인을 받는다: 고칠 파일 경로 / 추가할
   한 줄 원문 / 바뀌는 부분만의 diff / 되돌리는 방법. 저장소 밖 설정 변경이므로 승인 없이
   진행하지 않는다. 무응답·주제 전환은 승인이 아니다.
3. **적용** — 승인 뒤에만 같은 명령에 `--apply`를 붙인다. 백업·반영·reload·로그 재적재
   확인과 실패 시 롤백은 스크립트가 한다.
4. **보고** — 무엇을 고쳤는지와, 시스템 프롬프트는 세션 시작 시 고정되므로 **새 세션부터**
   적용된다고 보는 것을 함께 알린다.

되돌릴 때는 `--rollback`에 백업 경로를 준다. `--apply`가 없으면 이것도 dry-run이다.

```text
python scripts/append_system_prompt.py --rollback "백업 경로" --apply
```

## 9. 레벨 2 — 그래프 실행

2절에서 레벨 2로 떨어진 요청만 이 절로 온다. 이 절의 본문은
[references/level2.md](references/level2.md)에 있다. 레벨 2로 판정한 뒤 노드를 설계하기 전에
읽는다 — 설계·계획 승인·`GRAPH.md`·실행·재개·완료 보고가 거기 있다.

## 10. 브레인스토밍 진입

무엇을 만들지·어떻게 풀지가 정해지지 않은 채 아이디어를 요구하는 요청이면 — "브레인스토밍하자",
"아이디어 좀 내보자", "뭘 만들면 좋을까", "방향 좀 잡아줘" — 워커를 고르기 전에 **먼저
"브레인스토밍을 진행해볼까요?"를 묻는다.** 애매할 때만이 아니라 **매번** 낸다 — 지금부터가
아이디어 회의라는 것을 양쪽이 같이 알아야 어디서 끊고 다음 게이트를 물을지 정해진다.

할 일이 특정된 요청("고쳐줘", "조회해줘", "추가해줘")은 여기 들지 않고 1절로 간다. 판정이
갈리면 물어본다. 요청이 단순해 보인다는 이유로 이 물음을 건너뛰지 않는다. 사용자가 동의하면
[references/brainstorming.md](references/brainstorming.md)를 읽고 그 절차를 따른다. 거절하면
1절로 간다.
