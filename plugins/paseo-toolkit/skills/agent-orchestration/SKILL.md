---
name: agent-orchestration
description: '작업 지시를 Paseo 워커 에이전트에 위임해 실행할 때 사용한다 — 프로필 선택과 값 이관, 활성/대기 큐 운영, 1뎁스 팬아웃, 다단 그래프(조사 → 병렬 분석 → 취합 → 토론 → 결론)의 설계·승인·실행·취합을 담당한다. 대상 파일을 열거나 첫 명령을 실행하기 전에 먼저 읽는다 — 작업 크기로 예외를 두지 않으며, 직접 착수한 뒤에는 위임할 것이 남지 않는다. TRIGGER: 사용자가 위임을 말하지 않아도 (a) 조사·분석·검토·구현·리팩터·디버깅·문서화·확인처럼 무엇을 해달라는 작업 지시를 받았을 때 — 대상이 파일 하나든 여럿이든, 단계가 하나든 여럿이든, 한 줄로 끝날 것 같아도 포함한다, (b) 구현과 검토처럼 역할이 갈리는 일을 함께 요청받았을 때, (c) "위임해줘", "워커한테 맡겨", "병렬로 돌려", "에이전트 여러 개 띄워", "나눠서 처리해줘", "동시에 조사해줘", "라우팅해줘", "오케스트레이팅", "그래프로 짜줘", "토론시켜서 결론 내줘", "파일별로 각각 맡겨", "검토도 같이 시켜" 중 하나를 말했을 때. SKIP: 사용자가 이 세션에서 직접 하라고 지시했을 때, 그리고 요청이 다른 스킬 소관일 때 — Paseo 앱 설치·연결·설정·장애는 paseo-help, 프로필 자체의 설계·등록·반영은 profile-setup, 고정 형태 위임(자문 1명·위원 2명·인수인계 1명)은 paseo-advisor·paseo-committee·paseo-handoff.'
---

# Agent Orchestration

작업 지시를 받으면 리더인 너는 직접 착수하지 않는다. 작업을 워커에 맡기고, 결과를 취합해
사용자에게 보고하는 것이 이 스킬의 절차다. 작은 일도 그에 맞는 싼 모델로 내리는 것이 이
라우팅의 목적이므로 크기로 예외를 두지 않는다.

절차는 순서대로 1절부터 7절까지다. 레벨 2로 떨어지면 9절로 간다. 8절은 사용자가
상시 라우팅을 요청했을 때만 읽는다.

## 리더가 직접 하는 일

워커가 원리적으로 못 하는 셋만 직접 한다.

1. 이 세션의 대화 맥락 자체를 근거로 삼아야 하는 일
2. 사용자와의 확인 문답
3. 워커 결과 취합과 사용자 보고

파일과 설정은 대상이 이 세션 것이어도 워커가 다룰 수 있다. 워커에 맡긴다. 예외로 사용자가
"이 세션에서 직접 하라"고 지시하면 그 요청은 직접 한다.

`Agent` 도구·`Workflow`·deep-research는 쓰지 않는다 — 프로필을 실을 수 없기 때문이다.
단 스킬이 그 도구들을 쓰라고 지시하면 그 스킬을 따른다.

## 1. 전제 점검

### 도구를 부를 수 있는 상태로 만든다

claude에서 Paseo 도구는 deferred다. 스키마를 먼저 로드해야 호출할 수 있다.

```text
ToolSearch select:mcp__paseo__list_profiles,mcp__paseo__create_agent,mcp__paseo__list_workspaces,mcp__paseo__send_agent_prompt,mcp__paseo__get_agent_status,mcp__paseo__cancel_agent
```

필요한 도구가 늘면 같은 형식으로 이어 붙인다. **이 호스트에 `ToolSearch`가 있으면** 그
줄로 스키마를 먼저 로드한다. 없으면 이미 노출된 도구 목록과 현재 검색 수단으로 같은
이름을 확인한다. Codex라고 해서 `ToolSearch`가 없다고 단정하거나 특정 모드를 강제하지
않는다. 이후 절차의 도구 이름(`list_profiles`, `create_agent`, `list_workspaces`,
`send_agent_prompt`, `get_agent_status`, `cancel_agent`)은 접두어 없이 쓴다.

Paseo 도구가 이 세션에 붙어 있지 않으면 연결이 불충족이다. 그 사실을 명확히 보고하고
사용자 전역 config를 스스로 고치지 않는다. 프로필 조회는 `list_profiles` 하나뿐이고
대응하는 CLI 명령이 없다. provider를 추측하지 않는다. (`daemon.mcp.injectIntoAgents`가
활성이어야 새 세션에 Paseo 도구가 주입된다. 없으면 기본은 false다. 연결이 실패하면 이미
승인된 범위 안에서 복구하고, 복구할 수 없으면 필요한 설정 변경을 구체적으로 제시해 사용자
판단을 받는다.) 사용자 판단을 받은 뒤에만 `paseo run`의 `--provider`·`--mode`·`--thinking`으로
기동한다. 기동 후 응답·`inspect`의 `thinking`/`currentModeId` 값을 요청값과 대조해 적용
여부를 확인하고, 확인됐는지 못 했는지를 기동 보고(7절)에 적는다.

### 프로필 목록을 읽는다

`list_profiles`로 모든 프로필의 `notes`를 읽고 `notes`가 맞는 프로필을 고른다. 선택 기준은
`notes`에 있으므로 추측하지 않는다. 같은 세션에서 이미 읽었으면 다시 호출하지 않는다.

| 상태 | 판별 | 처리 |
| --- | --- | --- |
| **A. 목록이 비었다** | `list_profiles`가 빈 목록 | provider를 추측하지 않는다. 프로필이 없다고 알리고, 진행을 원하면 `list_providers`·`list_models`로 고른 뒤 그 사실을 보고에 남긴다 |
| **B. 맞는 것이 없다** | 어느 `notes`도 이 작업 유형을 포함하지 않는다 | 어느 프로필도 억지로 고르지 않는다. provider를 추측하지 않는다. 어떤 유형을 찾았고 어느 `notes`도 걸리지 않았는지 한 줄로 알린 뒤 `list_providers`·`list_models`로 고르고 그 사실을 보고에 남긴다 |
| **C. 부분적으로만 맞는다** | `notes`가 인접하지만 경계 문구가 이 작업을 배제한다 | 가장 가까운 프로필을 고르고 경계에서 벗어나는 부분을 명시해 알린다. provider를 추측하지 않는다. 벗어나는 부분이 작업의 본체이면 B로 취급한다 |

### 워크스페이스를 고른다

`list_workspaces`로 조회해 `cwd`가 대상 저장소인 워크스페이스를 고르고, 그 `workspaceId`를
`create_agent`에 **명시한다.** 생략하면 새 워크스페이스가 만들어진다. 워크스페이스 ID를
하드코딩하지 않고 매번 조회한다.

산출물 파일을 서로 읽어야 하는 작업들은 같은 워크스페이스에 둔다. 격리하면 뒤 작업이 앞
작업의 파일을 볼 수 없다. 같은 파일을 여럿이 써야 하는 요청은 2절 Step 3에서 레벨 2로
떨어지므로 격리로 우회하지 않는다 — 격리는 병합 문제를 새로 만든다. worktree 격리는 사용자가
명시할 때만 쓰고, 그때는 각 worktree 경로를 받아 그 경로를 읽어야 하는 워커의 브리핑에 넣는다.

## 2. 레벨 판정 게이트

작업을 쪼개기 전에, 워커를 하나라도 띄우기 전에 이 절을 통과한다.

**순서가 있는 판정 트리다. 위에서부터 내려가 처음 걸리는 곳에서 멈춘다.** 같은 요청이 두
레벨로 떨어지지 않게 하기 위해서다. **워커 수는 어느 단계에도 나오지 않는다** — 개수는 활성
큐와 대기 큐가 처리한다(3절).

```text
Step 0 (레벨과 직교, 항상 수행)
  비가역 점검 → 걸리면 그 작업에 별도 게이트를 단다. 레벨은 바꾸지 않는다.

Step 1  어떤 작업의 입력이 다른 작업의 출력인가?            예 → 레벨 2 (멈춤)
Step 2  같은 대상에 서로 다른 역할이 배정되는가?            예 → 레벨 2 (멈춤)
          (구현과 검토, 토론자와 사회자, 작성과 감수)
Step 3  둘 이상의 작업이 같은 파일을 쓰는가?                예 → 레벨 2 (멈춤)
Step 4  어떤 작업의 수용 조건 판정이 다른 작업의 결과에      예 → 레벨 2 (멈춤)
        걸려 있는가?
Step 5  Step 1~4 중 하나라도 "모르겠다"인가?                예 → 레벨 2 (멈춤)
Step 6  그 밖의 전부                                       → 레벨 1
```

- **트리는 망라적이고 배타적이다.** Step 6이 나머지 전부를 받으므로 모든 요청이 정확히 한
  레벨로 떨어진다.
- **Step 5가 애매함을 흡수한다.** 게이트 비용은 왕복 1회지만 잘못된 팬아웃은 파일 충돌과
  중복 작업으로 되돌리기 비용이 크다.
- **작업 개수는 레벨을 바꾸지 않는다.** Step 1~5가 전부 "아니오"면 개수가 몇이든 레벨 1이다.
- 쪼갠 결과 다른 작업이 없으면 Step 1·3·4가 성립하지 않고 Step 2도 역할이 하나뿐이라 성립하지
  않는다. 그때도 트리를 그대로 타고 Step 6에서 레벨 1이 된다 — 트리를 건너뛰는 예외 조항을
  두지 않는다.

### 레벨 2로 떨어지면 9절로 간다

Step 1~5 중 하나에 걸리면 **레벨 1로 처리하지 않는다.** 쪼개기·프로필 선택·기동 어느 것도
3절로 내려가지 않는다. 어느 Step에 무엇 때문에 걸렸는지 한 줄로 알리고 9절로 간다.
순서를 바꿔 순차로 돌리는 것으로 레벨 2가 레벨 1이 되지는 않는다.

### Step 0 — 되돌리기 어려운 작업

레벨과 직교하므로 **레벨 1 경로에서도, 워커가 하나뿐인 요청에서도 건너뛰지 않는다.** 레벨 2
조건에 묶어 두면 레벨 1 경로가 게이트를 통째로 건너뛴다.

범위는 넷이다 — git 커밋·푸시·태그·브랜치 삭제, 외부 전송, 파일·디렉터리 삭제, 저장소 밖
설정 변경.

- 걸리는 단위가 있으면 그 단위의 기동 **직전에** 승인을 받는다. 걸리지 않는 나머지 단위는
  승인을 기다리지 않고 기동한다.
- 사용자가 그 비가역 작업을 이미 명시적으로 지시했으면 그 지시가 곧 승인이다. 리더가 스스로
  추가한 비가역 행동은 언제나 승인 대상이다.
- git 커밋·스테이징·푸시·태그·브랜치 삭제는 사용자 명시 승인 없이 워커에 맡기지 않는다.
- 무응답·답변 누락·주제 전환·다른 작업 지시는 승인이 아니다.

## 3. 레벨 1 — 팬아웃

2절에서 레벨 1로 판정된 요청만 이 절로 온다. 레벨 1은 작업 사이에 의존이 없다는 전제 위에 선다.

1. 작업을 서로 겹치지 않는 단위로 쪼갠다. 단위 하나 = 워커 하나. 맥락이 다른 일은 나눈다 —
   한 세션에 여러 맥락을 몰지 않는다.
2. 단위마다 프로필을 고른다(4절). 단위 성격이 같으면 같은 프로필을 써도 된다.
3. 되돌리기 어려운 단위를 점검한다(2절 Step 0).
4. 활성 큐를 채워 기동한다. 승인 문답은 하지 않는다(비가역 단위 제외).
5. 완료 알림을 기다린다. 폴링하지 않는다. 알림마다 아래 투입 절차를 돈다.
6. 전원 완료 후 취합해 7절 형식으로 보고한다.

작업 개수가 많다는 것 자체는 승인 사유가 아니다. 개수는 큐가 처리한다.

### 활성 큐와 대기 큐

| 개념 | 값·정의 |
| --- | --- |
| **활성 큐** | 지금 실행 중인 워커. 동시 실행 상한 10 |
| **대기 큐** | 아직 기동하지 않은 작업. 상한 없음. 순서는 네가 쪼갠 순서(FIFO) |

작업마다 이만큼을 들고 있는다 — 작업 ID(쪼갠 순서) / 상태(`대기`·`실행 중`·`완료`·`실패`) /
프로필 / 대상 / 산출물 경로 / agentId(기동 후) / 비가역(예·아니오) / 재시도 가능 여부(6절).

**큐 상태 파일을 만들지 않는다.** 큐는 네 컨텍스트에만 둔다.

투입 절차 — 완료 알림 1건을 받으면:

1. 그 작업의 상태를 `완료` 또는 `실패`로 바꾼다. 활성 슬롯 1개가 빈다.
2. 대기 큐가 비어 있지 않으면 선두 1개를 기동하고 `실행 중`으로 바꾼다.
3. 알림이 여러 건 몰려 오면 **해제된 슬롯 수만큼** 투입한다. 상한을 넘겨 기동하지 않는다.
4. 대기 큐가 비고 활성 큐도 비면 취합·보고로 넘어간다.
5. 기동이 실패하면 그 작업은 활성 슬롯을 잡지 않은 것으로 보고 6절의 재시도 분류를 따른다.

### 의존을 뒤늦게 발견하면

대기 중 작업이 실패한 앞 작업의 산출물을 필요로 한다는 것이 드러나면, 그것은 실패 처리가
아니라 **레벨 판정이 틀렸다는 신호**다.

1. 남은 대기 큐의 투입을 멈춘다. 실행 중인 워커는 중단하지 않고 결과를 받는다.
2. 발견한 의존 관계를 명시해 알린다. "레벨 1로 판정했는데 작업 N이 작업 M의 산출물을 필요로
   한다"까지 적는다.
3. 남은 작업은 2절 Step 1에 걸린다 — 레벨 2로 재판정해 9절로 간다. 순차로 다시 배열해
   레벨 1로 이어 가지 않는다.
4. 이미 완료된 작업은 다시 기동하지 않는다. 그 산출물 경로를 새 그래프의 스테이지 1
   입력으로 재사용한다.

## 4. 프로필 선택과 값 이관

`create_agent`에는 `profile` 인수가 없다. **프로필의 값은 자동으로 적용되지 않으므로 네가
옮겨 담는다.** 이름이 서로 다르니 아래 대응을 그대로 따른다.

| 프로필의 값 | `create_agent`의 자리 |
| --- | --- |
| `provider` + `model` | `provider` — 두 값을 `provider/model` 한 문자열로 합쳐 넣는다 |
| `modeId` | `settings.modeId` |
| `thinkingOptionId` | `settings.thinkingOptionId` |
| `featureValues` | `settings.features` — **프로필에 이 키가 있을 때만** 옮긴다. 없으면 `settings.features`를 넣지 않는다 |

- `workspaceId`를 명시한다(1절).
- `title`은 `프로필이름: 한 줄 요약`, `labels`는 `{"profile": 프로필id, "task": 한 마디}`.
  `title`은 60자 상한이므로 그 안에서 만든다. 프로필 이름이 길어 요약 여유가 10자 미만이면
  `title`에 요약만 쓰고 프로필은 `labels.profile`로만 남긴다.
- `settings.modeId`는 스키마상 선택 필드지만 **채우는 것을 기본으로 둔다.** 프로필의 값을
  그대로 옮기는 것이 원칙이고, 채워서 나쁠 이유가 없다.
- **codex 워커의 실제 모드는 요청값과 다를 수 있다.** `create_agent` 응답의 `currentModeId`가
  실제 적용된 값이다 — 요청값과 같은지 대조해서 확인하고, 보고의 "모드" 줄에는 요청값이
  아니라 응답의 `currentModeId`를 쓴다.
- claude의 `plan` 모드는 파일 산출을 막는다. 산출물 파일을 써야 하는 단위에 고르지 않는다.
- 요청한 model이 프로필에 의해 덮어써진 사례가 있다. 어떤 모델로 돌았는지는 워커에게
  보고시킨다(5절).

## 5. 워커 브리핑

워커 제약을 매 단위마다 실을 수 있는 자리는 `initialPrompt` 하나다. 그래서 워커 규칙 전부가
여기 들어간다.

`assets/worker-briefing.md`가 그 템플릿이다. **워커를 띄우기 직전에 읽고** 중괄호 자리를 채워
`initialPrompt`로 넘긴다.

반드시 실어야 하는 것:

- **첫 줄 고정** — `너는 워커다. 프로필: {이름} — {그 프로필의 notes 원문}`. Paseo는 워커에게
  자기 프로필을 알려주지 않으므로 프롬프트에 써야 한다. `notes`의 경계 문구가 워커의 작업
  범위다.
- 작업 지시와 **원본 수용 조건**. 네가 요약한 것이 아니라 원본을 옮긴다.
- 산출물 경로와 실패 보고 경로(둘 다 네가 지정한다), 쓸 도구, 출력 크기 제한.
- 다시 위임하지 않는다. 셸은 기본 도구로 실행하고 터미널을 새로 만들지 않는다.
- 보고 형식(결론 / 근거 위치 `파일:줄` / 미검증 항목)과, 어떤 모델·모드로 돌았는지.
- 실패했으면 **실패 보고 경로**에 실패 사유를 쓰고 수정 대상 파일에는 쓰지 않는다. 네가 파일만
  보고 실패를 판정할 수 있어야 한다.

리더 절차(전제 점검, 큐 운영, 승인 게이트, 보고 형식)는 브리핑에 넣지 않는다. 워커가 쓸 일이
없는 맥락이고, 실으면 워커가 다시 위임하려 든다.

### 검토는 만든 워커에게 맡기지 않는다

산출물의 수용 판정을 그것을 만든 에이전트에게 맡기지 않는다. 구현자가 브리핑한 검토는
구현자의 전제를 물려받아 같은 오류를 통과시킨다.

- 검토는 **검토용 `notes`를 가진 프로필의 별도 워커**에 맡긴다. 구현 워커와 다른 프로필이다.
- 넘기는 것은 **원본 수용 조건과 대상 경로**다. 구현자의 보고서는 넘기지 않는다.
- 구현과 검토를 함께 요청받은 것은 2절 Step 2에 걸린다. 레벨 2이므로 9절로 간다. 검토
  노드를 띄우기 직전에 [assets/review-briefing.md](assets/review-briefing.md)를 읽고
  중괄호를 채워 `initialPrompt`로 넘긴다.

### 산출물은 파일로 받는다

완료 알림에 실리는 응답은 잘릴 수 있다. 그래서 결과는 알림 본문이 아니라 파일로 받는다.

- 조사·분석처럼 결과가 텍스트라면 실행 디렉터리를 정한다:
  `.skywork/paseo-orchestration/{날짜}-{슬러그}/`. 작업마다 산출물은 `nodes/{작업 ID}.md`,
  실패 보고는 `nodes/{작업 ID}.failure.md`에 쓰게 한다 — 반드시 서로 다른 파일이다.
  경로는 네가 정해 브리핑에 명시한다. 스킬 소스에 사용자의 고정 경로를 하드코딩하지
  않는다 — 실행 중에는 선택한 workspace 기준 상대 경로로 해석한다. worktree 간 전달이나
  호스트가 절대 경로를 요구할 때는 조회로 확인한 실제 절대 경로를 그대로 전달한다.
- 산출물이 파일 수정 그 자체라면 성공했을 때 쓸 별도 산출물 파일을 만들지 않는다. 그래도
  **실패 보고 경로는 지정한다** — 같은 실행 디렉터리의 `nodes/{작업 ID}.failure.md`다. 성공하면 그
  파일이 생기지 않고, 파일이 있으면 그것이 실패다.
- **수정 대상 파일에 실패 사유를 쓰게 하지 않는다.** 그 파일이 곧 산출물이므로 실패 기록을
  섞으면 산출물이 망가진다. 실패 사유는 언제나 실패 보고 경로로 간다.
- **산출물 본문을 네 컨텍스트로 옮기지 않는다.** 리더 컨텍스트를 아끼는 것이 위임의 나머지
  절반이다. 판단이 필요할 때만 알림 요약을 읽고, 부족하면 산출물 파일을 범위를 제한해 읽는다.

## 6. 실행 중 개입과 실패 처리

실패 분류·재시도 한도·셸 실패·프로바이더 장애·재개의 동작은 그때
[references/failure-handling.md](references/failure-handling.md)를 읽는다. 아래는 그 자리에서
빠지면 행동이 바뀌는 게이트다.

### 주입 — 승인이 필요한 것과 아닌 것

실행 중인 워커에 지시를 보내는 수단은 `send_agent_prompt`다. 그 워커를 폐기할 때만
`cancel_agent`를 쓴다. **이 호스트가 실행 중 메시지·모드 변경을 막으면 그 정책을 이 표보다
앞세운다.** 막히면 우회하지 않고 보고한다.

| 종류 | 예 | 승인 |
| --- | --- | --- |
| **조회형** | 진행 상황 질의, 지금까지 쓴 산출물 경로 확인 | 불필요 |
| **범위 내 명확화** | 산출물 경로 재지시, 출력 형식 지정, 이미 쓴 산출물의 처리 지시(덮어쓰기/이어 쓰기/버리기) | 불필요 |
| **범위 변경** | 수용 조건의 변경·추가, 대상 파일 추가, 역할 변경, 비가역 행동 추가, 권한을 올리는 모드 변경 | **필수 — 보내기 전에** |

승인 없이 보낸 뒤 사후에 알리면 게이트를 우회한 것이다. 애매하면 범위 변경으로 본다.
이미 받은 계획·비가역·권한 승인은 같은 범위에서 다시 묻지 않고 재사용한다. 레벨 2에서
범위 변경을 승인받으면 보낸 뒤 `GRAPH.md`의 해당 노드 행을 갱신한다(9절).

### 실패

| 상황 | 처리 |
| --- | --- |
| 워커 실패 | 참조의 재시도 분류를 따른다. 무조건 자동 재시도는 하지 않는다 |
| 브리핑 결함으로 실패 | 재시도하지 않고 원인과 함께 보고한다 |
| 권한 대기 | 기존 승인 범위 안이면 재사용해 응답한다. 없거나 범위를 넘으면 요청 원문을 그대로 보여주고 사용자 판단을 받는다 — 임의로 자동 승인하지 않는다 |
| 명시 거부 | 재시도하지 않는다. 모드를 올려 우회하지 않는다 |
| 사용자 중단 | 실행 중 전원을 중단하고 작업별 상태와 남은 산출물 경로를 보고한다 |
| 부분 실패 | 작업별 상태(완료/실패/미기동) + 완료된 산출물 경로 + 무엇을 못 했는지 |

자동 재시도는 참조의 두 조건(부수효과 없음 + 멱등)을 모두 만족할 때만, 1회만 한다. 호출
**전에** 사용 횟수를 기록한다. 생성 결과가 불명이면 재기동하지 않고 조회부터 한다. 실패
보고 경로에 내용이 있으면 그것을 완료보다 앞세운다. 레벨 2는 재시도 셀에 한도와 사용
횟수를 적는다(9절).

### codex 셸 · 프로바이더 장애

셸 실패를 `full-access`로 자동 승격하지 않는다. 호스트 정책이 허용하고, 그 권한이 이미
승인됐거나 새로 승인된 뒤에만 모드를 바꾼다. 프로바이더 장애는 자동 대체하지 않는다.
동작은 참조를 따른다.

## 7. 취합과 보고

워커를 띄운 뒤 작업마다 아래 불렛을 출력한다. 작업이 여럿이면 작업별로 줄을 갈라 적는다.

```text
이름 : {Paseo 프로필상 이름}
모델 : codex/gpt-5.6-luna [ {thinkingOptionId} ]
모드 : {create_agent 응답의 currentModeId}
작업 : 현재 Git 상태 조사
```

- **"모드"는 `create_agent` 응답의 `currentModeId`를 그대로 쓴다.** 요청값을 쓰면 사실과 다르다(4절).
- `paseo run` CLI로 기동했으면 `thinkingOptionId`가 유실될 수 있다는 사실을 함께 적는다(1절).
- 대기 큐가 있으면 `활성 N / 대기 M`을 함께 낸다.
- **쪼개기와 프로필 선택의 과정은 출력하지 않는다.** 결과만 낸다.
- 레벨 2는 승인 시점에 그래프를, 완료 시점에 노드별 상태와 취합 결과를 낸다. 설계 과정은
  출력하지 않는다.

전원 완료 후 취합해 보고한다. 취합과 보고는 워커에 맡기지 않는다.

- 작업별 상태(완료/실패/미기동)와 완료된 산출물 경로를 낸다.
- 워커가 자기 보고에 적은 모델 값이 네가 요청한 값과 다르면 보고의 모델 줄에 출처를 표시한다 —
  요청한 model이 프로필에 의해 덮어써진 사례가 있다.

## 8. 세션이 이 스킬을 참조하지 않고 지나갈 때

기본은 `description`만으로 발동하는 것이다. 그래도 작은 단일 작업에서 세션이 스킬을 보지 않고
직접 착수하는 누락이 남는다. 그 누락은 문장을 고쳐 없앨 수 있는 것이 아니므로, 데몬 상시
프롬프트에 라우팅 한 줄을 심어 메운다. 한 번 발동한 실행이 이후 세션의 상시 규칙을 설치하는
부트스트랩이다.

**사용자가 이 폴백을 요청했을 때만 실행한다.** 대상은 `~/.paseo/config.json`의
`daemon.appendSystemPrompt` 하나이고 다른 후보를 두지 않는다.

`scripts/append_system_prompt.py`가 백업·적용·reload·롤백을 맡는다. **이 절차를 실행할 때
읽는다.** 기본이 dry-run이라 승인 전에 안전하게 제시할 수 있고, 값을 덧붙이기만 하며 제거
경로가 없다.

1. **점검·제시** — `--value`에 추가할 한 줄만 주고 실행한다. 그 줄이 이미 있으면
   `alreadyPresent`가 참으로 오고 아무것도 하지 않는다. 그 사실을 알리고 끝낸다.
   ```text
   python scripts/append_system_prompt.py --value "추가할 한 줄"
   ```
2. **승인** — dry-run이 낸 넷을 그대로 보여주고 명시 승인을 받는다: 고칠 파일 경로 / 추가할
   한 줄 원문 / 바뀌는 부분만의 diff / 되돌리는 방법. 저장소 밖 설정 변경이므로 승인 없이
   진행하지 않는다. 무응답·주제 전환은 승인이 아니다.
3. **적용** — 승인 뒤에만 같은 명령에 `--apply`를 붙여 다시 실행한다. 백업 생성 →
   `daemon.appendSystemPrompt`만 교체 → `paseo daemon reload` → stdout이
   `Configuration reloaded.`인지 대조 → 데몬 로그 재적재 확인까지 스크립트가 한다. 반영에
   실패하면 스크립트가 백업으로 되돌린다.
4. **보고** — 무엇을 고쳤는지와, 시스템 프롬프트는 세션 시작 시 고정되므로 **새 세션부터**
   적용된다고 보는 것을 함께 알린다.

되돌릴 때는 `--rollback`에 백업 경로를 준다. `--apply`가 없으면 이것도 dry-run이다.

```text
python scripts/append_system_prompt.py --rollback "백업 경로" --apply
```

## 9. 레벨 2 — 그래프 실행

2절에서 레벨 2로 떨어진 요청만 이 절로 온다. 새 스케줄러나 실행 엔진을 만들지 않는다.
기동은 `create_agent`, 순서는 완료 알림과 아래 절차다. 값 이관은 4절, 브리핑은 5절,
주입·실패 게이트는 6절이다.

그래프 형태(팬아웃·팬인·순차·토론)를 고를 때는
[references/graph-patterns.md](references/graph-patterns.md)를 읽는다.
실패·재시도·재개·프로바이더 장애의 동작은 그때
[references/failure-handling.md](references/failure-handling.md)를 읽는다.

같은 요청의 중단·압축 복구면 아래 「재개」로 간다. 새 요청이면 앞선 그래프를 재사용하지
않고 다시 설계한다.

### 설계

스테이지는 `>>`로, 같은 스테이지의 병렬 노드는 `|`로 잇는다.

```text
1. 조사
   >> 2-1. 분석A | 2-2. 분석B | 2-3. 분석C
   >> 3. 취합
   >> 4-1. 토론자1 | 4-2. 토론자2 | 4-3. 토론자3
   >> 5. 결론
```

노드 표가 승인 대상이자 실행 표다. 두 벌을 만들지 않는다.

| 필드 | 내용 |
| --- | --- |
| 노드 ID | `2-1`처럼 스테이지-순번 |
| 프로필 | 이름과 선택 근거 한 마디 |
| 입력 | 앞 노드 산출물 **경로** (스테이지 1은 리더 브리핑) |
| 산출물 | 성공 결과 경로 + 실패 보고 경로 — 워커 브리핑(5절)에 준 경로와 같아야 한다 |
| 수용 조건 | 관찰 가능한 한 줄 |
| 워크스페이스 | 공유(local) 또는 격리(worktree) |
| 비가역 | 예/아니오. 예면 그 노드 기동 직전 추가 승인(2절 Step 0) |
| 재시도 | `불가` 또는 `가능 · 한도 1 · 사용 0`. 열을 늘리지 않는다. 한도와 사용 횟수는 이 셀에 적는다 |
| 상태 | `대기` / `실행 중` / `완료` / `실패` |
| agentId | 기동 성공 직후 채운다 |

- 같은 파일을 쓰는 노드는 격리하지 말고 그래프를 고쳐 순차로 둔다.
- 기본 워크스페이스는 1절에서 고른 공유 workspace다. worktree는 사용자가 명시할 때만.
- 토론 라운드 전역 상한은 없다. 설계 때 스테이지로 드러나게 지정한다.
- 한 스테이지 노드 수가 10을 넘으면 3절 큐를 그 스테이지 안에서 그대로 쓴다.

### 계획 승인

워커를 하나라도 띄우기 **전에** 계획 승인을 **1회** 받는다. 읽기·조사만인 다단 그래프도
같다. 개별 노드 기동에 추가 계획 승인은 없다. 이미 받은 승인은 그래프 형태가 그대로면
재사용한다.

보여줄 것: 스테이지 표기 + 노드 표 전문 + 총 워커 수 + 아래 `GRAPH.md` 경로 — 이것과 승인
요청을 **일반 응답으로 출력하고 턴을 마쳐** 사용자 답변을 기다린다. 표 전체 검토가 필요하므로
이 승인 요청은 질문 도구의 짧은 선택지로 대체하지 않는다.
무응답·주제 전환은 승인이 아니다. 승인 전에 `create_agent`를 호출하지 않는다.

형태가 바뀌는 재설계·범위 변경 주입·프로바이더 대체는 다시 승인받는다.

### GRAPH.md

레벨 2는 항상 만든다. 생성 자체는 승인 대상이 아니고, 승인 요청에 **경로만** 알린다.

경로: `.skywork/paseo-orchestration/{날짜}-{슬러그}/GRAPH.md`
산출물: 같은 디렉터리의 `nodes/{노드 ID}.md`(성공) · `nodes/{노드 ID}.failure.md`(실패) —
반드시 서로 다른 파일이다
스킬 소스에 사용자의 고정 경로를 하드코딩하지 않는다 — 실행 중에는 선택한 workspace 기준
상대 경로로 해석한다. worktree 간 전달이나 호스트가 절대 경로를 요구할 때는 조회로 확인한
실제 절대 경로를 그대로 전달한다.

기록 시점 — 중간 압축에 대비해 경계만 기다리지 않는다.

1. 최초 생성: 승인된 스테이지 표기 + 노드 표. 상태는 `대기`, agentId는 비움.
2. `create_agent` **성공 직후**: 그 행의 `agentId`와 상태를 `실행 중`으로.
3. 완료 판정 **직후**: 상태를 `완료` 또는 `실패`로. 재시도할 때는 **호출 전에** 재시도 셀의
   사용 횟수를 고친다.
4. 스테이지 경계: 그 스테이지 전원 상태를 확정해 기록한 뒤에만 다음으로 간다.
5. 승인된 주입이 노드 행을 바꾸면 그때도 갱신한다.

### 실행

1. **기동 전에** 그 노드의 산출물 경로와 실패 보고 경로가 서로 다른 파일인지 확인한다 —
   같으면 분리하기 전까지 기동하지 않는다. 승인된 한 스테이지의 `대기` 노드를 기동한다.
   상한 10 + FIFO는 3절과 같다.
2. 완료 알림을 기다린다. 폴링하지 않는다.
3. **종료와 수용 성공을 나누어 판정한다.** 실패 보고 경로에 내용이 있으면 `실패`다.
   그다음에야 산출물과 수용 조건을 대조한다. 에이전트가 멈춘 것만으로 `완료`가 아니다.
4. 그 스테이지의 `대기`가 없어지고 전원이 `완료`여야 다음 스테이지로 간다. 하나라도
   `실패`면 그 노드에 의존하는 노드는 기동하지 않는다.
5. 뒤 노드 브리핑에는 앞 노드 산출물의 **경로만** 넣는다. 본문을 붙이지 않는다.
6. 검토 노드는 구현 노드와 **다른 프로필**이다. 띄우기 직전에
   [assets/review-briefing.md](assets/review-briefing.md)를 읽고 채운다. 넣는 것은 원본
   수용 조건과 대상 경로뿐이다. 구현 보고서 텍스트는 넣지 않는다.
7. 실행 중 `agentId`가 있으면 그 ID로 상태를 조회한다. 살아 있거나 이미 끝난 노드를
   완료 재기동하지 않는다. 생성 결과가 불명이면 조회부터 하고 중복 기동하지 않는다.
8. 주입과 실패 게이트는 6절이다. 범위 변경·재설계는 보내기 전에 승인을 받는다. 호스트가
   실행 중 메시지를 막으면 보내지 않고 보고한다.

### 재개

`GRAPH.md`를 읽고, 기록된 `agentId`로 실제 에이전트를 조회하고, 산출물·실패 보고 파일과
대조한다. `완료`이거나 아직 실행 중인 노드는 다시 띄우지 않는다. `실패`·`대기`만 기동한다.
그래프 형태가 그대로면 이미 받은 계획 승인을 재사용한다. 대조 절차는 참조의 「재개」다.

### 완료 보고

전원 판정이 끝나면 7절로 간다. 노드별 상태와 취합 결과를 낸다. 설계 과정은 출력하지 않는다.
