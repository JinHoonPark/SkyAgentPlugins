# AGENTS.md

이 저장소는 개인용 에이전트 플러그인 마켓플레이스다.
지원 대상 에이전트는 **claude(Claude Code)와 codex(Codex CLI) 두 개**이며,
두 에이전트의 마켓플레이스 매니페스트와 플러그인 소스를 한 저장소에 담는다.

이 문서는 **모든 작업에 공통으로 적용되는 운영 규칙**만 담는다.
제품의 목적·동작·수용 조건, 마일스톤 계획, 작업 메모는 여기에 쓰지 않는다.

---

## 1. 권위 순서

문서·계획·코드가 서로 어긋날 때는 아래 순서가 이긴다.

1. 사용자가 현재 내린 명시적 지시
2. 제품 스펙의 목표와 수용 조건
3. 실제 코드, 매니페스트, 스키마, 테스트 결과
4. 현재 마일스톤 계획
5. 과거 계획과 작업 메모

충돌이 생기면 **새 문서를 만들어 봉합하지 않는다.** 상위 권위에 맞춰 하위 문서를 수정하거나 폐기한다.
계획은 언제든 수정·폐기할 수 있으며, 구현보다 높은 권위를 갖지 않는다.

## 2. 승인 규칙

- 사용자의 명시적 승인이 없는 제안은 확정되지 않은 것으로 취급한다.
- 무응답, 답변 누락, 주제 전환, 다른 작업 지시는 승인이나 합의가 아니다.
- 미승인 제안을 실행하거나 확정 사항 및 계획의 전제로 사용하지 않는다.
- 판단이 필요한 것만 제안으로 올린다. 선택지가 둘 이상인 결정, 요청 범위를 넘는 변경,
  문서가 약속한 동작의 변경, 되돌리기 어렵거나 외부에 영향을 주는 변경이 여기 해당한다.
  검증까지 끝난 결함에 수정 방법이 하나뿐이면 실행하고 결과로 보고한다.

## 3. 문서 규칙

문서 구조를 최대한 작게 유지한다. 아래 규칙은 예외 없이 적용한다.

- 사용자의 요청 없이 새 계획서, 계약서, 추적 문서, 인수인계 문서를 만들지 않는다.
- 문서 간 섹션 ID 기반 계약이나 상호 인용 체계를 만들지 않는다.
- 계획과 코드가 충돌하면 보조 문서로 모순을 봉합하지 않는다.
  충돌한 가정, 코드 근거, 제안 변경을 사용자에게 보고한다.
- 설계 결정 기록(ADR)은 다음 세 조건을 **모두** 만족할 때만 작성한다.
  1. 여러 대안이 실제로 존재했다.
  2. 선택 결과가 장기간 영향을 미친다.
  3. 나중에 선택 이유를 모르면 되돌릴 위험이 크다.
- 실행 가능한 수용 조건은 문서가 아니라 테스트로 표현한다.

## 4. 작업 범위

- 현재 task 범위를 넘어선 문제는 구현하지 않고 후속 항목으로만 보고한다.
- 변경한 모든 줄은 요청에 근거가 있어야 한다. 인접 코드·주석·포맷의 임의 개선은 하지 않는다.
- 요청한 결과를 내는 데 필요한 원인 수정은 범위에 포함한다.
  요청과 무관한 기존 결함과 죽은 코드는 고치지 않고, 현재 작업의 정확성이나
  검증에 영향을 주는 것만 보고한다.
- 결과와 범위가 같은 더 단순한 방법은 선택하고 결과에 적는다.
  결과나 범위가 달라지는 방법은 만들기 전에 먼저 말한다.
- 완료 여부는 체크박스가 아니라 실행된 코드와 검증 결과로 판단한다.

## 5. 저장소 레이아웃

```
.claude-plugin/marketplace.json      claude용 마켓플레이스 매니페스트
.agents/plugins/marketplace.json     codex용 마켓플레이스 매니페스트
plugins/<plugin-name>/
  .claude-plugin/plugin.json         claude용 플러그인 매니페스트
  .codex-plugin/plugin.json          codex용 플러그인 매니페스트
  skills/<skill-name>/
    SKILL.md                         스킬 진입점 — 두 에이전트 공용
    scripts/                         실행 코드 (읽지 않고 실행 가능)
    references/                      필요할 때만 읽는 상세 문서
    assets/                          산출물에 쓰이는 파일 (템플릿·이미지·폰트)
    agents/openai.yaml               codex 표시용 메타 (선택)
  scripts/                           여러 스킬이 공유하는 스크립트만
  commands/ agents/ hooks/ .mcp.json claude 전용 컴포넌트 (codex는 읽지 않음)
```

두 에이전트의 매니페스트 스키마는 서로 다르다. 형태는 이렇다.

```jsonc
// .claude-plugin/marketplace.json
{ "name": "sky-agent-plugins", "owner": { "name": "..." }, "description": "...",
  "plugins": [ { "name": "x", "source": "./plugins/x",
                 "description": "...", "keywords": ["category:workflow"] } ] }

// .agents/plugins/marketplace.json
{ "name": "sky-agent-plugins", "interface": { "displayName": "..." },
  "plugins": [ { "name": "x", "source": { "source": "local", "path": "./plugins/x" },
                 "category": "workflow" } ] }

// plugins/x/.claude-plugin/plugin.json
{ "name": "x", "version": "1.0.0", "description": "...", "author": { "name": "..." } }

// plugins/x/.codex-plugin/plugin.json  — skills 필드가 있어야 스킬이 로드된다
{ "name": "x", "version": "1.0.0", "description": "...", "author": { "name": "..." },
  "skills": "./skills/" }
```

- 이름은 kebab-case를 쓰고, **플러그인 디렉터리 이름 · 두 `plugin.json`의 `name` · 두 마켓플레이스 매니페스트의 항목 이름**을 전부 일치시킨다.
- `version`과 `description`도 두 `plugin.json` 사이에서 일치시킨다.
- **플러그인 버전은 `main`에 머지하는 시점에 올린다.** 변경 규모를 판정해 major·minor·patch 중
  무엇을 올릴지와 추천 버전을 사용자에게 알리고, 승인받아 반영한다. 에이전트가 임의로 정해
  올리지 않는다.
  - major: 기존 사용 방식이 깨지는 변경 (스킬 삭제·개명, 명령이나 옵션의 호환성 깨짐)
  - minor: 기능 추가 (새 스킬, 새 옵션)
  - patch: 결함 수정, 문서 수정, 내부 정리
- 개발 중 캐시 갱신을 위해 올린 값은 릴리스 버전이 아니다. 머지 시점에 규모에 맞는 값으로 맞춘다.
- 플러그인을 추가·삭제·개명하면 **네 개의 매니페스트를 같은 커밋에서** 갱신한다.
  한쪽 에이전트에만 등록된 상태로 커밋하지 않는다.
- 예외로 **한 에이전트 전용 플러그인**은 그쪽 매니페스트 두 개만 둔다.
  전용으로 두는 이유를 아래 목록에 적는다. 이유가 적히지 않은 반쪽 등록은 결함이다.
  - `codex-skill-creator` — Anthropic 공식 마켓플레이스의 skill-creator를 codex용으로
    컨버팅한 것이다. claude는 같은 스킬을 공식 플러그인으로 이미 제공하므로,
    claude에 등록하면 내용이 다른 skill-creator 두 개가 겹친다(§6과 같은 이유).

## 6. 스킬 작성 규칙

스킬이 이 저장소의 기본 단위다.

- **사용자가 다른 방식을 지시하지 않는 한, 스킬의 신규 제작과 개정은 그 에이전트의 `skill-creator`로 한다.**
  - claude: 공식 플러그인 `skill-creator`
  - codex: 설치 시 함께 들어오는 `~/.codex/skills/.system/skill-creator`
  구조·분량·frontmatter 규격은 그 스킬이 가지고 있다. 이 문서에 옮겨 적지 않는다.
  두 문서는 이름이 같아도 내용이 다르다. 한쪽에 맞춰 다른 쪽 파일을 고치지 않는다.
- `skill-creator`가 없는 에이전트에서 스킬을 만들거나 구조를 바꿔야 하면
  아래 골자만으로 진행하고, 규격 전체를 따르지 못했다는 사실을 사용자에게 알린다.
  - SKILL.md 본문은 500줄 미만으로 둔다. 넘으면 실행 코드는 `scripts/`,
    상세 문서는 `references/`, 산출물에 쓰이는 파일은 `assets/`로 분리한다.
  - 분리한 파일은 SKILL.md에서 링크하고 **언제 읽어야 하는지** 함께 적는다.
    참조 깊이는 SKILL.md 기준 한 단계까지.
  - frontmatter는 `name`과 `description`만 쓴다. "언제 쓰는가"는 전부 `description`에 넣는다.
    본문은 스킬이 발동한 뒤에만 읽히므로 본문의 "이럴 때 사용" 섹션은 효과가 없다.
  - 스킬 폴더에 `README.md`, `CHANGELOG.md` 같은 부가 문서를 만들지 않는다(§3와 같은 원칙).

### 이 저장소만의 규칙

- 두 에이전트 공용 플러그인의 핵심 동작은 스킬에 둔다.
  `commands/`, `agents/`, `hooks/`, `.mcp.json`은 claude에서만 동작하므로,
  이것들에만 존재하는 기능을 만들면 codex에서 그 플러그인은 반쪽이 된다.
- 스킬 본문은 두 에이전트가 함께 읽는다. 에이전트별로 본문을 복제하지 않는다.
- `description`의 트리거에는 사용자가 실제로 쓰는 한국어 표현을 넣는다.
- 스킬을 작성하다 다른 스킬 폴더의 파일을 쓰게 되는 순간, 그 파일을 플러그인 루트의
  `scripts/`로 즉시 옮긴다. 스킬끼리 파일을 참조하는 상태로 남겨두지 않는다.

## 7. 코딩 규칙

- 스킬 본문에서 파일을 참조할 때는 **스킬 디렉터리 기준 상대 경로**로 지시한다.
  `${CLAUDE_PLUGIN_ROOT}`는 codex에 존재하지 않으므로 claude 전용 컴포넌트에서만 쓴다.
- 스크립트는 그 스킬 안(`skills/<name>/scripts/`)에 둔다.
  둘 이상의 스킬이 공유할 때만 플러그인 루트의 `scripts/`로 올린다.
- 플러그인 루트의 공용 스크립트는 스킬 기준 상대 경로 `../../scripts/`로 부른다.
- 사용자 홈 경로, 드라이브 문자, 저장소 절대 경로를 하드코딩하지 않는다.
- 매니페스트는 표준 JSON이다. 주석과 trailing comma를 넣지 않는다.
- 훅과 스크립트는 실패해도 세션을 중단시키지 않게 만든다.
  기대한 입력이 없으면 조용히 통과시키고, 진단은 stderr로 남긴다.
- 일어날 수 없는 상황에 대한 에러 처리를 추가하지 않는다.
- 내 변경 때문에 쓰이지 않게 된 것만 제거한다.

### Windows 인코딩

- `.md`, `.json`, `.ps1`, `.py`는 UTF-8(BOM 없음)로 쓴다.
- `.bat` / `.cmd`에는 한글을 쓰지 않는다. cmd.exe가 CP949로 읽어 깨지며,
  파일 안의 `chcp 65001`로는 해결되지 않는다.
- 한글이 들어간 `.ps1`은 pwsh 7 기준으로 작성한다.

## 8. 검증 규칙

- 무엇으로 검증할지 **먼저** 정하고, 실행한 명령과 결과를 보고한다.
  긴 출력은 판단에 필요한 부분만 인용한다.
- 매니페스트를 고쳤으면 **두 에이전트 모두**에서 확인한다. 한쪽만 보고 끝내지 않는다.
  - claude: `claude plugin validate <경로>` — 플러그인/마켓플레이스 매니페스트와
    스킬·에이전트·명령을 검증한다.
  - codex: `codex plugin list`에 해당 플러그인이 나오는지 확인한다.
    `codex plugin marketplace upgrade`는 Git 마켓플레이스로 배포한 뒤의 절차다.
    배포해야 실행할 수 있는 것을 개발 단계의 검증 조건으로 넣지 않는다.
- 스킬처럼 로드가 필요한 것은 이 저장소를 로컬 경로 마켓플레이스로 등록해 설치한 뒤 확인한다.
  등록은 두 에이전트 모두 `plugin marketplace add <저장소 경로>`로 한다.
  - claude: `plugin install <플러그인>@<마켓플레이스>`로 설치한다. 재설치 대신
    `plugin update <플러그인>`을 쓸 수 있고, `--plugin-dir <플러그인 경로>`로 설치 없이
    그 세션만 로드할 수도 있다.
  - codex: `plugin add <플러그인>@<마켓플레이스>`로 설치한다. `--plugin-dir`에 대응하는
    방법이 없으므로 codex 쪽 확인은 항상 이 설치로 한다.
  플러그인 본체는 각 에이전트 캐시로 복사되므로, 저장소를 고친 뒤 반영하려면
  `plugin.json`의 `version`을 올리고 다시 설치한 뒤 새 세션에서 확인한다.
- 훅·명령과 설치 상태에 의존하는 것은 **설치본이 갱신된 상태에서** 확인한다.
  저장소 파일만 고친 상태로는 실행 중인 세션에 반영되지 않는다.
- 테스트가 있으면 버그 수정은 재현 테스트를 먼저 만들고 통과시킨다.
  리팩터링은 전후 모두 green이어야 한다.
- 어느 것도 불가능했다면 "검증하지 못했다"고 명시한다.
  검증하지 않은 변경을 완료로 보고하지 않는다.

## 9. 금지 사항

- 사용자의 명시적 승인 없는 git 커밋, 스테이징, 푸시, 태그, 브랜치 삭제.
- 토큰·자격 증명·개인 식별 정보를 저장소에 커밋하는 것.
- 설치본(`~/.claude/plugins`, `~/.codex/plugins/cache` 등)을 직접 수정해 문제를 우회하는 것.
  원본은 항상 이 저장소이고, 설치본은 갱신으로만 바꾼다.
- 요청 없이 외부 서비스에 저장소 내용을 업로드하거나 배포 파이프라인을 추가하는 것.

## 10. 보고 형식

- 응답은 한국어로 쓴다. 코드, 명령, 기술 용어는 원문을 유지한다.
- 근거는 `파일:줄` 형식으로 인용한다.
- 충돌을 발견하면 세 가지를 함께 보고한다: 충돌한 가정 / 코드 근거 / 제안 변경.
- 규칙을 지키는 것으로 충분하다. 지킨다고 선언하지 않는다.
  조항 번호는 그 규칙 자체가 논의 대상일 때만 꺼낸다.
